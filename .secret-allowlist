# Self-reference: secret detection script contains patterns as string literals
scripts/check-public-repo-secrets.py

# KNOWN_REPOS list legitimately contains repo names that match blocklist literals
# (auto_pb_swing_trader, tradebook_GAS as canonical SST3 KNOWN_REPOS entries).
# Self-reference: the public-mirror utils file MUST contain these names to keep
# parity with canonical KNOWN_REPOS — they are not secret leaks.
scripts/sst3_utils.py
# sst3_mirror_utils.py contains a sample-fixture string that exercises the
# path / issue-url / repo-ref scrubbers — the KNOWN_REPOS names appear inside
# the test-fixture sample, not as actual references. Same exemption rationale
# as sst3_utils.py. (#460 Stage 5 — surfaced when 75-file mode-sync produced
# a real edit on this file.)
scripts/sst3_mirror_utils.py
# check-crossrepo-paths.py docstring uses KNOWN_REPOS names as illustrative
# examples of the cross-repo-path failure mode it detects. Same exemption
# rationale as sst3_utils.py / sst3_mirror_utils.py.
scripts/check-crossrepo-paths.py

# SST3 documentation references private dotfiles issues as provenance
# These are intentional cross-references in the public mirror
reference/self-healing-guide.md
standards/ANTI-PATTERNS.md
standards/STANDARDS.md
scripts/README.md

# Test files contain path patterns as test fixtures
tests/test_path_portability.py

# Wrapper-lane allowlist (added during dotfiles#456 Stage 5 — when the wrappers
# + test-fixtures + self-test infrastructure were first mirrored to this repo).

# sst3-check.sh and sst3-sync-tool-eviction.sh contain eviction-token literals
# that construct mcp__<tool-name>__ patterns at runtime to drive the
# tool-eviction sweep. The literals are NOT secrets; they're the search target.
scripts/sst3-check.sh
scripts/sst3-sync-tool-eviction.sh

# Test-fixtures intentionally contain fake secrets / API_KEY references / token
# literals as INPUT to the secret-detection wrappers (the whole point of these
# fixtures is to exercise sst3-code-secrets.sh / sst3-sec-* scanners).
#
# These WERE six directory-form entries (`test-fixtures/<name>/`). Allowlist
# path matching is now segment-anchored to a FILE (hoiboy-uk#56: the old
# `entry in file_str` half let an entry travel to unrelated paths), which made
# every directory-form entry inert. Replaced with the exact files that produce
# findings, category-scoped so the rest of each file is still scanned. The
# three fixture dirs that produced ZERO findings when measured
# (sec-input-sources, sec-deserialize, sec-subprocess) are dropped rather than
# carried as dead config that would silently cover a future real finding.
test-fixtures/code-config/input/consumer.py::GENERIC_SECRET
test-fixtures/code-secrets-leak/input/leaky.txt::GENERIC_SECRET
test-fixtures/sec-secret-touchpoints/input/touchpoints.py::GENERIC_SECRET
test-fixtures/sec-secret-touchpoints/input/touchpoints.py::PLATFORM_TOKEN

# check-stage1-research-fields.py parses Stage-1 research markdown bullets; its
# local var `first_token = _strip_md_bold(raw.split("(")[0])...` matches the
# GENERIC_SECRET `*token =` heuristic but is a markdown-parser variable, not a
# secret assignment. File-level exemption matches the sst3_utils.py / mirror-utils
# convention above. (dotfiles#523 AC 1.5 — newly-mirrored generic framework script.)
scripts/check-stage1-research-fields.py
