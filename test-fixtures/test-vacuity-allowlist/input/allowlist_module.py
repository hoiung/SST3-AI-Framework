"""Fixture: the measured Issue #3 allowlist-overreach case — a bare
literal key whose one reason 'covers' dozens of occurrences, many of them the
structured Model=4 identifier the reason does not describe. Replicates the
SHAPE with a constructed 32 occurrences / 20 structured in doc.md; the source
measurement reconciled to 33/24 (#567 T3 F9 — figures here are synthetic by
construction, not the measured pair)."""

UNBOUND_LITERALS = {
    "4": "ordinals and list numbering",
    "2026": "year in dates",
}
