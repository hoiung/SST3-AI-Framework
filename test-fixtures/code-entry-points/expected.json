{
  "fixture": "code-entry-points",
  "issue": 447,
  "phase": 8,
  "scope": "Entry-point discovery. app.py: 1 double-quote main (if __name__ == \"__main__\") + 1 @app.route + 1 @app.get http_handler. app_sq.py (#547 Stage-4 #447 recall gate): 1 single-quote main (if __name__ == '__main__') — RED pre-fix (double-quote pattern missed it, absent from output) — plus a != inverse-guard negative control that must NOT be flagged. Asserts 4 records, both files present.",
  "wrapper": "sst3-code-entry-points.sh",
  "args": ["python"],
  "cwd": "input",
  "expect": {
    "exit_code": 0,
    "stdout_min_records": 4,
    "stdout_must_contain_files": ["app.py", "app_sq.py"]
  }
}
