# Leader Mode

Direct instructions from the human leader. NOT suggestions. Orders. Do not skim. Do not summarise. Do not skip steps. Execute exactly as written.

## Guardrails (Apply to ALL Stages)

**SST3 script path — resolve `$SST3` FIRST, in the same shell call**:

```bash
SST3=$( [ -f scripts/load-stage-rules.sh ] && echo scripts/ || echo "<your-dotfiles-clone>/scripts/" )
```

Then invoke every SST3 script as `bash $SST3/<script>.sh ...`. A bare `scripts/...` exits 127 from any consumer repo; a fixed `$HOME` path silently reads the MAIN dotfiles clone even from a dotfiles worktree, so canon edits in flight are not seen. The resolver picks the local canon when one exists and falls back otherwise. Confirm exit 0. **A 127 here is a path failure, not a missing inner engine.** If the fallback branch also 127s, derive the root from the installed command symlink: `dirname $(dirname $(readlink -f ~/.claude/commands))`.


- **Context budget**: Research phase <30% of context. On long work, when remaining nears ~50% (~500K of 1M) the agent AUTO-runs `/handover` + compact + continue; never operate below 50% remaining.
- **Post-compact recovery**: If context was compacted mid-stage, re-read this skill file's active `/Leader` stage section line-by-line + the handover file in full (if `~/handover/current-task.txt` is present) + CLAUDE.md + STANDARDS.md + STANDARDS.md §Per-Stage Feedback Capture (in-flight workflow telemetry) + the active Issue before continuing. Do NOT resume from memory — a pre-compact read does not survive a compact (memory is diluted; re-read, do not assume).
- **Branch safety**: NEVER switch branches. Commit and push to the CURRENT branch. Ask the user if you need something from main.
- **Commit discipline**: Commit per file. NEVER `git add -A` or `git add .`.
- **AP #16 Monitor**: Every command, script, test, commit, push — verify end-to-end. Check exit codes, tail logs, confirm side effects. "Started" is not "done".
- **Subagent RESULT block**: Every subagent prompt MUST require a fenced `## RESULT` block at the end of its response:
  ```
  ## RESULT
  mcp_graph_available: yes|no   (FIRST line when subagent discusses graph queries — AP #19)
  verdict: pass|fail|unknown
  files_touched: [list]
  findings: [{path, line, claim, evidence}]
  tee_log: <path or "none">
  scope_gaps: [list or "none"]
  evidence_command: <exact shell command>   (MANDATORY for any angle whose deliverable is a COUNT, INVENTORY, or BYTE-SIZE — AC 1.6)
  evidence_stdout: <verbatim output (or NOT_RUN if untestable)>
  ```
  Prose-only numbers are not verifiable — if you cannot paste the `evidence_command` + its `evidence_stdout`, state NOT_RUN (AC 1.6).
- **Scope snippet**: When dispatching >=10 subagents, write a frozen scope snippet (<=2K tokens) to `/tmp/sst3-scope-<topic>.md` and pass the PATH to each subagent — not the full issue body. **Scope-snippet completeness checklist (AC 1.4)** — the frozen snippet MUST carry: [ ] **Structural decisions section**: cross-repo branch model, orchestration choices (for issues touching >=2 repos); [ ] **Verbatim operator constraints**: the 1-2 most load-bearing constraints embedded literally; [ ] **Known design limits appendix**: previously-documented out-of-scope items and why; [ ] **Binary decision gates**: any yes/no choices that block implementation, surfaced explicitly at the top. If scope expands mid-swarm, write a `scope-expansion-delta` sub-block to the research file (what was added, why, date) — do NOT silently fold mid-stage scope changes into the main findings.
- **Double-Guardrail Principle (N32)**: every `/Leader` invocation checks BOTH the cross-cutting SST3 canonical (STANDARDS.md + ANTI-PATTERNS.md + WORKFLOW.md + project CLAUDE.md) AND the invoked-skill's domain canonical (voice_rules, eBay rules, blog voice-guard, voice-doc-repo dual-lens, claude-api prompt caching, project CLAUDE.md rules). Stage 1 identifies the invoked skill and records `invoked_skill` + `skill_canonical_files` in the research file. Stage 3 adds a skill-canonical compliance subagent angle. Stage 4 Gate 1 + Stage 5 run the skill's own verification hooks. See STANDARDS.md "Double-Guardrail Principle".
- **Multi-layer discipline (AP #14)**: Minimum 2 layers of subagents. Layer 2 MUST use a DIFFERENT prompt/angle than layer 1. Main agent verifies EVERY finding against source before acting. Swarm recommends; main agent verifies; source decides.
- **Never Assume**: Read the actual source before drawing conclusions. Do not state file contents without reading. Do not assume variable names, API shapes, or function signatures from memory. When in doubt, read first, conclude after.
- **Fix Everything**: All problems found get fixed — no deferrals, no scope excuses, no language boundaries. Python, Rust, JS, SQL, YAML, shell — fix them all. Only valid skip: confirmed false positive (document why).
- **FACT-CHECK RULE**: Default position: doubt yourself. Unless a claim is 100% backed by evidence, it is NOT factual and MUST be treated as unverified. Do not trust your memory. Do not trust your own confidence. Memory can be diluted, summarised incorrectly, or outdated. A memory entry without reference links, evidence, or calculations proving how figures were derived is NOT a reliable source. If memory says X but you cannot find the file, line, command output, or calculation that proves X, then X is UNVERIFIED — go find the proof or retract the claim. When in doubt, doubt yourself — then go verify.
- **Governance Enforcement — Checkbox MCP (AP #20)**: at every Acceptance Criteria checkbox completion → invoke `mcp__github-checkbox__update_issue_checkbox(issue_number, checkbox_text, evidence)` with per-deliverable evidence. Comment-only progress is NOT a substitute. **Before invocation**: check for deferred-tool status and load the schema via `ToolSearch` per STANDARDS.md "MCP Tool Schema Loading" — mandatory, not optional. Canonical evidence-quality patterns: `../reference/tool-selection-guide.md` Example 2.

## Stage Selection

If the user provided a stage number as an argument (e.g. `/Leader 3`), go directly to that stage. Treat `/Leader 6` as a deprecated alias for `/Leader 5` — silently proceed to Stage 5. If no argument, present this menu and WAIT:

```
Leader Mode — pick a stage:

1. Research     — subagent swarm to research a topic, findings to /tmp
2. Issue Draft  — prepare /tmp issue from research, full quality sweep
3. Sanity Check — triple-check scope vs audit, then create GitHub issue
4. Implement    — /SST3-solo, implement all phases, Verification Loop, Ralph Review, merge, user-review-checklist
5. Post-Implementation Review — subagent swarm post-implementation deep audit (100% mandatory)
```

Which stage? (1-5)

---

## Stage 1 — Research (Subagent Swarm -> /tmp)

**Stage 1 mandatory-reading directive (#498 Phase 4 AC 4.8)**: run `bash $SST3/load-stage-rules.sh 1` to load the Stage-1-tagged canonical subset (research / subagent-swarm / pre-swarm SEED rules). The loader replaces the prior "read STANDARDS.md in full" directive; per-stage tagging emits ~28K bytes vs the full ~50K canonical. Always-load carve-out (privacy / voice / destructive-op) is included automatically.

Scale subagent count to match scope. No maximum. No minimum of 2-3. If 12 directories to check, dispatch 12+ subagents. Every subagent covers a different angle, directory, file set, or data source. No two subagents share the same prompt.

**Process**:
0. **Restate the task in the user's exact words** at the TOP of the research file, before any other content. Quote the user's original phrasing verbatim. If your interpretation requires translating jargon, preserve the original alongside your interpretation. Rationale: an intent error cannot be caught by any graph query or subagent angle — only pre-swarm restatement can. Stage 3 sanity-check verifies the restatement matches the user's literal task before approving scope. **Named-entity fidelity (AC 3.1):** any specific entities, values, or sets the operator enumerates (e.g. `MB100/MBS100`, a named file list, specific tickers/IDs) are preserved VERBATIM in the restatement; NEVER broaden a named SET to "all" / "uniform" / "every" / "any" as a simplification — the enumeration IS the scope (the inverse of AP #14e; canonical rule: STANDARDS.md "Named-Entity Scope — Don't Broaden a Named Set").
0a. **Identify invoked skill + record skill-canonical metadata** (N32 Double-Guardrail first link). Record in the research file: `invoked_skill: <skill-name-or-"none">` and `skill_canonical_files: [list of files]`. Examples — `blog` / `voice-doc-repo` → `voice_rules.py`, `voice-doc-repo/VOICE_PROFILE.md`; `ebay-seller-tool` → Seagate HARD CONTRACT + 21-field listing contract docs; `claude-api` → prompt-caching SDK references + current model IDs; `SST3-solo` / `Leader` → N/A (cross-cutting SST3 only, no domain skill). If task is pure infrastructure (no domain skill invoked), record `invoked_skill: none`. This metadata is READ by Stage 3 (skill-canonical compliance subagent angle), Stage 4 Gate 1 (skill-canonical verification checkbox), and Stage 5 (skill-canonical audit). See STANDARDS.md "Double-Guardrail Principle". **Primary driver-repo selection (AC 3.9):** which repo owns the active skill canonical? `dotfiles` is reserved for SST3-framework issues, not domain work that merely uses SST3 — file domain work in the consumer repo that owns the canonical.
0b. **Prior-feedback ground** (per STANDARDS.md §Per-Stage Feedback Capture): read aggregator output via `bash $SST3/leader-feedback-aggregate.sh --report` (markdown — pull "Pending Improvements" + "Top 5 Improvements per Stage" sections). For topic-keyword overlap, run `bash $SST3/leader-feedback-aggregate.sh --shape-match <kw1>,<kw2>` and pull top 1-3 referenced feedback files. Pull `improvement` field where `improvement_status: pending` into the Stage 1 /tmp scope file as 'patterns to apply / pitfalls to avoid'. When a prior `pending` improvement is satisfied by this run, close the loop with `bash $SST3/mark-improvements-applied.sh` (the canonical tool — it sets `improvement_status: applied` AND writes a STANDALONE `**applied_in**: <this-issue>` field line). The `applied_in` marker is ALWAYS its OWN field line — NEVER hand-cram `applied_in: ...` inside the `improvement_status` value (that produces a non-enum value the feedback parser rejects with exit 1, and the file then sits un-aggregatable until fixed). Token budget ~12K total (report ~3K + at most 3 prior feedback files at ~3K each). Skip-cleanly when index empty (mechanism not yet activated, or first issue post-activation).
0c. **Chat Reconciliation — Agreements Log + early verifier pass (AC 6.1; doctrine: STANDARDS.md "Chat Reconciliation (Verifier-Led)").** Maintain a `## Agreements Log` section in the research file: extract the operator's raw typed messages with `python3 $SST3/extract-chat-agreements.py --issue <N>` and append an agreement bullet (with its transcript ref) AS each agreement is made (captured-when-agreed, not reconstructed-late; do NOT duplicate the verbatim restatement from step 0 — link it). Then run ONE neutral independent-verifier pass (the cheapest/earliest drift catch): dispatch a fresh-context subagent given ONLY the extracted messages + agreements log (it is NOT shown your proposed scope) and the neutral prompt "from these messages, what did the operator ask for / agree to / rule out?", and double-check its independent interpretation against your proposed research scope — flag `invented` / `dropped` / `inverted` divergence before the swarm. (The full THREE-MODEL Haiku+Sonnet+Opus panel + operator sign-off run at Stage 3 step 2a and Stage 5; Stage 1 is a single early pass.) The `--issue <N>` flag (dotfiles#528 AC 5.3) selects THIS issue's worktree session transcript by slug — required because a bare-parent (canonical-clone) project dir accumulates many issues' sessions; without it the tool now FAILS LOUD on that ambiguity rather than silently picking latest-by-mtime (which could be another issue's session).
1. Check `docs/research/` in the active repo for existing research FIRST. Do not re-derive what exists. **Non-code-framing live probe (AC 1.9)**: for any scope framing that asserts a non-code or operator-side classification ("not a code issue", "operator-side state", "false positive on contract X", or a code-grep concluding a flag is "never set"), dispatch ONE verifier subagent that probes the live system (grep dynamic construction patterns: `f"{...}_<suffix>"`, `setattr`, dict-key f-strings; DB/log probe) BEFORE accepting the framing. A live DB row exhibiting the flag is authoritative over any "never set" code-grep result.
1a. **MANDATORY self-test + status CHECK at Stage 1 top** (#447 Phase 4 update): run `bash $SST3/sst3-self-test.sh` FIRST — this is the wrapper-lane regression gate. ANY drift line in the NDJSON (`{"kind":"fixture-drift",...}`) means a wrapper has regressed against its frozen known-answer fixture; **ABORT swarm dispatch immediately**, do not proceed to subagent recall. Recovery procedure: open `solo/wrapper-fix-<bug>` branch (NOT this Issue's branch), reproduce the failing fixture, fix the wrapper, run the self-test until `0 drifted`, push the fix branch through Stage 4 in its own Issue, then resume the original `/Leader` invocation here. After self-test passes, run `bash $SST3/sst3-code-status.sh` and record `last_updated` + `file_count` in the research file — audit trail for Stage 5 reviewers, even on 12-moments topics. The wrapper-lane is stateless; every query re-parses on disk, so there is no staleness, no build step, no null/0 decision rule (those applied to the displaced upstream MCP). `sst3-code-update.sh` exists only as a no-op contract-preservation shim. For full A+B+C+D audit (code + doc + sync at once), invoke `/sync-check` skill OR `bash $SST3/sst3-check.sh --all` (Phase D Layer-2 orchestrator). **Stage-recipe reference (#484 W6.6)**: the per-stage wrapper-lane command set (Stage 1 seed / Stage 2 scope-completeness / Stage 3 counter-query / Stage 4 freshness / Stage 5 audit) is canonically enumerated in `docs/guides/code-query-playbook.md` "Stage-Mapped Recipes" — consult it for the exact recipe rather than re-deriving. **Doc-lane diff-trigger (#484 W6.1)**: the DOC lane (`sst3-doc-*`) is diff-triggered, NOT `graph_applicable`-gated — if this Issue's diff touches `*.md` / frontmatter it runs regardless of the code-SEED classification (canonical rule + rationale: WORKFLOW.md Stage 1 "Doc-lane is diff-triggered, NOT graph-gated").
    **Error path (G-1 fix, #444; wrapper-lane note Issue #445; semantics clarified by Issue #456)**: Under the wrapper-lane (Issue #445+), MCP-transport errors no longer apply; wrappers exit with code 127 + the documented stderr contract instead. **Post Issue #456**: exit 127 means the inner engine is **genuinely missing on disk** (npm/cargo/pipx install never ran). Pre-#456 the same code ALSO fired when the engine was on disk but PATH was not propagated to non-interactive shells; that PATH-propagation gap is now closed by `sst3-bash-utils.sh` self-bootstrap. Run `<your-dotfiles-clone>/scripts/install.sh` to install missing engines — do NOT add custom PATH workarounds in the calling agent. (Retained for historical context: in the MCP-native phase, distinguish transport-layer disconnect from normal error response. If `config status` returns `MCP error -32000: Connection closed` or stdio-EOF symptom → graph disconnected at the transport layer. See `../docs/guides/code-query-playbook.md` "Connection Closed / Disconnected — Recovery".) For wrapper-lane calls: check stderr; if a wrapper exits non-zero, document `[GRAPH unavailable: <reason>]` in the research file and proceed via subagent-only research per `SST3-solo.md` fallback. "MANDATORY" refers to the CHECK being unconditionally attempted, NOT to halting the workflow when the tool is unavailable — fallback is documented and Ralph-acceptable.
1a.5. **PRE-SWARM SOURCE-VERIFICATION GATE for handover claims** (Theme 1, #477): if a pre-research handover or post-compact recovery summary exists at session entry, main agent MUST extract 3-5 headline claims (file:lines, counts, table sizes, "X doesn't exist" assertions, named functions / hooks / scripts said to exist) from the handover and verify each via raw-grep / Query / direct read BEFORE dispatching the swarm. Document any drift in the /tmp research file as "claim X corrected from Y to Z before swarm dispatch". Memory ≠ source of truth; handover ≠ frozen contract. Rationale: pre-swarm verification catches scope-amendment cycles that would otherwise force mid-Stage-2 AC rewrites or a Stage-4 false-positive bug-hunt. Cross-reference: STANDARDS.md "Never Assume — Always Check" → "User Assertion = Immediate Source Verification" sub-rule, Rule 1b ("Handover claim → grep source immediately"); ANTI-PATTERNS.md AP #14d (Scope-gap blindness — Stage 1 research specific). **Three explicit verification classes (AC 1.7):** (1) **Closure-rationale claims** — grep each architecture claim (e.g. "X mechanism handles Z") before accepting scope; (2) **Pre-existing classification** — fetch the cited Issue body + commit list and verify scope overlap before accepting "pre-existing #N" framing; (3) **Fix ranking** — the Layer-2 adversarial prompt (step 2a) MUST include a named sub-angle: challenge the handover's fix ORDERING against source, not just factual accuracy. If the handover's core hypothesis is mechanically testable (exit code, guard presence, scanner output), run the ONE decisive command BEFORE writing the Issue contract and record the result in the /tmp research file §0.5.
1b. **Pre-swarm wrapper-lane SEED**: if the research topic is structural code in a supported language (see STANDARDS.md "Structural Code Queries"), use the wrapper-lane to DEFINE scope BEFORE dispatching the swarm — not to verify pre-formed scope AFTER. Run `bash $SST3/sst3-code-callers.sh <symbol> <lang>` / `bash $SST3/sst3-code-callees.sh <function_name> <lang>` / `bash $SST3/sst3-code-search.sh '<pattern>' <lang>` on every symbol the USER MENTIONED, plus `bash $SST3/sst3-code-impact.sh <base-branch>` on user-named files. Feed the resulting call-site / symbol / blast-radius data into subagent prompts so layer-1 angles are scoped to real evidence, not hypothesis. Wrapper-lane findings SEED the swarm; they do NOT replace its different-angle coverage. **Hybrid topics** (code + docs): run wrapper-lane queries on the code portion ONLY; use subagents for the semantic portion. Record `graph_applicable_files` vs `graph_exempt_files` in the research file. Skip-condition: if topic is semantic / voice / intent / cross-document / non-code (12 subagent-only moments per STANDARDS.md), skip wrapper-lane queries and go to step 2 — the 1a status CHECK still ran unconditionally for audit trail. Record wrapper-lane calls + `last_updated` + `file_count` + one spot-check file:line per query in the /tmp research file. (`search` is keyword-only — there are no embeddings; verify with synonym sweep before any "no match" conclusion.) **Cross-validation rule (#447 Phase 5 — wrapper-lane vs raw recall delta gate)**: BEFORE dispatching the swarm, cross-validate the wrapper-lane SEED against ONE raw-tool subagent on the same query (e.g. wrapper `sst3-code-callers.sh foo python` vs subagent grep `^def foo|foo(`). Compare counts. Delta ≤20%: trust the SEED, dispatch the planned swarm. Delta >20%: wrapper-lane is SUSPECT for this query class — dispatch matched wrapper+raw subagent pairs for layer 1 (one subagent per pair) so the swarm covers both signals; record the delta + final reconciliation in the research file. The 20% threshold is empirical (#445 R4 wrappers landed at 0% delta against raw on structural angles; pre-R4 sat at 17% large-fn recall which is why a 20% bound surfaces drift early). **Empty-SEED fallback trigger (AC 1.11):** if the SEED result is empty for a symbol the task description asserts exists, immediately run `grep -rln <symbol>` as a raw cross-check. If the delta between SEED count and raw grep count is >20%, use the raw call-site map to scope subagent angles (the wrapper is unavailable for this query class). Record `pre_swarm_graph_seed: skipped (reason: <wrapper-unavailable|empty-delta>)` in the /tmp research file §0 frontmatter. Note: Rust + async + multi-file symbol topics default to skip-clean on the SEED; record the reason explicitly.
1.5. **Live-environment baseline (AC 1.9):** for tasks on a specific host or deployment, capture a live-environment baseline (`lscpu`, `free -h`, `df -h`, service status, AppX/scheduled-task state) into `/tmp/host-baseline.txt` and pass its path to every subagent angle as context. Static analysis of a host-specific task without a captured baseline is an unverified framing.
2. Author + run the research swarm via the Workflow tool (DEFAULT) — max 5 files per subagent/subtask. Include the RESULT block schema in every subtask prompt. (Agent/Task dispatch is the documented fallback ONLY for a trivial single-angle check.)
   **Swarm fan-out discipline (AC 1.10):** If the scope or operator brief enumerates analytical angles by name, that count is the HARD MINIMUM for subagent fan-out — do not collapse to family-level sweeps. Derive directory/file coverage clusters from the full top-level `ls` output of every named directory BEFORE authoring the swarm — never hand-list a subset. Synthesis subtasks MUST be dispatched AFTER all swarm outputs are on disk — the synthesis subtask prompt must name the input files it reads, and those files must exist before the subtask runs.
   **Prior-art / already-implemented standing angle (AC 1.12 — MANDATORY for every swarm):** dispatch one Layer-1 subagent whose sole task is to grep the consumer repos and the canonical harness for the mechanism name and its synonyms — confirm whether it already exists before proposing it as new work. For changes that fan out to consumers, query `SST3/drift-manifest.json` `mirrors` array for the per-file consumer count rather than asserting a uniform N. For install/bootstrap topics, enumerate all entry points (`install.sh`, `provision.sh`, `*.ps1` equivalents) BEFORE proposing new install steps.
   **Runtime-path sub-task (AC 1.13):** for any angle whose question is "what does X reference at runtime" (vs statically), include an explicit sub-task to trace file-copy / symlink / template-deploy steps to the runtime target before drawing path-dependency conclusions — static analysis alone misses copy-to-path indirections.
   For named domain-class + situational research angles, see `standards/stage-1/domain-angle-catalogue.md` (AC 1.2).
2.1. **Marker-enumeration angle (AP #24) — CONDITIONAL** (Theme 2, #477 AC 4.3): for any Issue introducing, modifying, or removing a marker substring (error-message partition, counter name, diagnostic flag, feature-gate literal, status-enum value, log-line prefix, partition key), include a dedicated marker-enumeration subtask in the authored Workflow (DEFAULT), or dispatch an Agent/Task subagent (trivial-single-angle fallback), with the prompt: "Find every site in the codebase where this exact substring `<literal>` is emitted, referenced, or checked. Use `grep -rn -F '<exact_literal>' src/ tests/ scripts/ --include='*.py'` (or per-language equivalent) BEFORE any implementation. List every match with file:line + triage (emission / fixture / mock / stale)." Record count + per-site triage in Issue body as "Known Emit Sites: (N)". Stage 4 Gate 1 will re-run the same grep and gate on count-drift (`WORKFLOW.md` Verification Loop "Marker-substring enumeration (AP #24)" checkbox). Skip-clean if Issue does NOT introduce/modify/remove a marker substring. Cross-reference: ANTI-PATTERNS.md AP #24 (canonical rule), STANDARDS.md "Marker-Substring Discipline".
2a. **Layer-2 adversarial gap-finder MANDATORY for Stage 1 swarms** (Theme 8, #477; AP #14 instantiation): after the Layer-1 swarm completes, include a Layer-2 adversarial gap-finder subtask in the authored Workflow (run after the Layer-1 subtasks; Agent/Task = trivial-single-angle fallback) with the explicit prompt: "Layer-1 found X, Y, Z. Find 3 things they missed — either (a) false-positive claims that are already covered by modern equivalents (e.g. legacy registry key vs Win11 24H2 modern equivalent; SetUserFTA legacy API vs UserChoice ProgId modern path), or (b) genuine gaps not yet surfaced (scope adjacent to Layer-1 angles but uncovered)." For infrastructure / governance / cross-cutting Issues this angle is load-bearing — Layer-1 generic-angle coverage misses scope-adjacent gaps that Layer-2 different-prompt sweeps catch. Layer-2 prompt MUST differ from Layer-1 (per AP #14c — different angle, different blind spots). Cross-reference: STANDARDS.md "Stage 1 Layer-2 Adversarial Gap-Finder Discipline" + ANTI-PATTERNS.md AP #14d. **Required structural angles (AC 1.8)** — the Layer-2 prompt MUST include these 7 named angles when applicable: (1) **hot/cold path enumeration** — for each data-consumption site enumerate (a) cold-path validation, (b) hot-path validation, (c) revalidation interval; flag any path with (a) lacking (b); (2) **async/sync compatibility** — for any recommended reuse target, verify the async/sync context matches the call site; (3) **operator-semantics grep** — verify `>=`/`>`/`<=`/`<` in threshold claims against source code, not prose; (4) **tool-disambiguation** — when the operator names a specific product/tool, one angle must verify exact feature identity against official docs; (5) **self-disconfirmation** — list 2-3 simplest scenarios under which the alleged gap does NOT exist; verify each against source; (6) **Sibling-pattern enumeration (AP #14e — Issue #54)** — for any finding about a function, pattern class, or family member, enumerate ALL callers / branches / sibling files that could share the same defect class before returning a verdict: extract the CONCEPT and use concept-based grep, not literal-pattern grep. The literal-pattern sweep finds only what already shares your vocabulary, so a family member named differently survives the sweep untouched — which is exactly how Issue #52 shipped a miss that Stage 5, where this instruction WAS wired, then caught. Mirrors the Stage-5 **Sibling-pattern enumeration (AC 5.3)** angle; the two are deliberate twins — edit both or neither; (7) **Source-as-consumer enumeration (AP #30 — Issue #54)** — when a producer and its consumer share no token, the producer is itself a candidate consumer. Enumerate paired surfaces by asking "what else emits or consumes this fact?", never by grepping the producer's own name; a token-blind pair escapes both the AP #24 literal-marker grep and the AP #14e pattern-class sweep. Mirrors the Stage-3 **Producer-surface enumeration** angle below, which fires too late to scope Stage-1 research.
3. Main context = orchestrator ONLY. Do NOT read source files directly. Subagents read; main agent collates.
4. If subagents return uncertain or conflicting findings, dispatch MORE subagents to resolve. Do not guess. **When Layer-2 surfaces false-positive corrections to Layer-1 claims** (e.g. Layer-2 says "Layer-1 cited legacy API X, but modern equivalent Y exists in Win11 24H2"): main agent must verify the equivalence against source BEFORE accepting the correction; document the verification method inline (command run, file:line, URL). Both Layer-1 and Layer-2 outputs are subagent recommendations; the source decides (per AP #14c). Pattern (#477 Theme 8): main agent dispatches a verification subagent with the prompt: "Run `grep / ls / Query` against <file>:<line> to confirm whether <legacy claim> or <modern equivalent> is present in the actual codebase. Report file:line + verbatim quote." Drift between Layer-1, Layer-2, and source = block scope-acceptance until reconciled.
5. Collate ALL findings into `/tmp/research_<topic>_<date>.md`. File MUST contain:
   - **Findings** — each backed by file path, line number, command output, or URL
   - **Gaps identified** — what the research did NOT cover or could not confirm
   - **Plan / ideas / angles / idea-repo** — based on facts and data, not opinions or assumptions. Brainstorm with subagents to generate new angles, then verify each angle against evidence before including it.
   - **References** — every claim has a provenance trail (file:line, command output, URL, calculation showing how numbers were derived)
   - **Truth-table temporal axis (AC 5.5)** — for any truth table in an AC, add a Temporal axis row (mid-flight | terminal) and enumerate all parameter modes (`full_history=True/False`, etc.). A truth table that omits the mid-flight-vs-terminal axis silently assumes terminal-only and misses the in-progress state class.
6. Present summary to user with `/tmp` file path. **Pre-filing hypothesis run (AC 1.3):** Before writing the Issue contract, run any mechanically testable hypothesis (exit code, guard presence, scanner self-test) that would collapse the scope — record the result in §0.5 of the research file. **Live-consumer gate (AC 1.5):** For each proposed AC, identify the live consumer — who reads this output, and what do they do with it? If no live consumer, exclude from scope.

**DO**: Back every claim with evidence. Dispatch more subagents when uncertain. Check existing research first. Brainstorm new angles with subagents. Verify every idea-repoed idea against evidence before including it. Record `graph_applicable: true|false (reason: <class>)` in the /tmp research file — downstream stages 2/3/4/5 MUST read this field and do NOT re-derive the classification independently (single-declaration-carries-forward, TB-2 N29+N34).
**DON'T**: Read source files in main context. Default to 2-3 subagents. State findings without file:line references. Hallucinate. Trust memory without verifying against source. State numbers without showing how they were calculated.

**Per-stage feedback (Layer C; per STANDARDS.md §Per-Stage Feedback Capture)**: write the Stage 1 block to the per-issue feedback file before declaring complete. 10 fields (model / worked / didnt / why / improvement / improvement_status / evidence / friction / rule_self_caught / rule_user_caught), soft-cap guidance per STANDARDS.md, no forward-preference phrasing.

**SIGN-OFF**: When done, tell the user: "Stage 1 (Research) complete. Next: `/Leader 2` (Issue Draft)."

---

## Stage 2 — Issue Draft (/tmp, NOT GitHub)

**Stage 2 mandatory-reading directive (#498 Phase 4 AC 4.8)**: run `bash $SST3/load-stage-rules.sh 2` to load the Stage-2-tagged subset (issue-template / acceptance-criteria / quality-mantras / AC-verifiability rules). Replaces full-canon read at Stage 2 entry.


Create `/tmp/issue_draft_<topic>_<date>.md` FIRST. Do NOT create the GitHub issue yet.

**Process**:
1. Read the `/tmp` research file from Stage 1 AND run `python3 $SST3/check-stage1-research-fields.py /tmp/research_<topic>_<date>.md` exit 0 BEFORE proceeding (#460 Phase 4 — Stage 1 metadata validator gate). Validator failure = block Stage 2 dispatch until the Stage 1 file declares `graph_applicable` + `invoked_skill` + `skill_canonical_files` per the carry-forward contract. **(AC 2.7)** Treat any validator failure as a hard block that returns to Stage 1 for fix-up (not a silent workaround). Common authoring errors: backtick-wrapped field values (backtick-wrapped `graph_applicable` value is WRONG; correct is unstyled YAML `graph_applicable: false`), freeform enum values not in the validator's domain (allowed: `false | hybrid | true` — do not invent synonyms), and YAML inline comments inside scalar values (parse failures). Fix at source before Stage 2 proceeds. **Pre-dispatch sub-checks (AC 3.2/3.16):** (a) operator-state — if the symptom is 'X stopped working' / 'silent failure of Y', grep `.<X>_state` files / journalctl Stopped entries / UI-toggle audit comments before dispatching; (b) equivalence trace — for every equivalence claim, trace the consumer one level past the stated producer and cite the exact transform line; (c) body-size — `wc -c` the draft body; if >64000 chars, design the body/comment split during Stage 2, not after `gh issue create` fails; (d) comment-walk — walk issue #N comments through <date> and stamp the frozen-scope file with the walk date; (e) JBGE-knob gate — for every new knob/constant with no current-problem evidence, mark `JBGE-DEFERRED` with a trigger condition; (f) implementer self-containment — verify a verbatim User Brief blockquote is present, no /tmp-only references; (g) cross-repo AP#22 — grep the draft for `cd <other-repo>` patterns and confirm AP #22 compliance + a pre-flight sentinel per downstream phase; (h) voice-mirror prerequisite — for any voice-guarded deliverable in a repo other than voice-doc-repo-doc-repo/hoiboy-uk/voice-staging, check whether the target repo appears in `SST3/drift-manifest.json` mirror-consumer list for the voice-scanner scripts (`check-iamhoi-wrapping.py`, `check-ai-writing-tells.py`, `voice_rules.py`); if not, add an AC 0.x to vendor the voice-scanner trio via `propagate-mirrors.py` before writing voice-gate verify commands.
1a. **Skeleton-first authoring (AC 3.5):** copy the full `issue-template.md` skeleton to `/tmp/issue_draft_<slug>.md` FIRST; fill scope into the skeleton — do NOT author a scope-only file and merge later.
1b. **Graph scope-completeness recipe** (structural-code topics in supported languages — see playbook "Stage-Mapped Recipes" Stage 2 section): for every function/hook/class named in the scope, run `bash $SST3/sst3-code-callers.sh <symbol> <lang>` and compare the count to what Stage 1 research claimed. More call sites than research flagged = expand scope. For every "X doesn't exist" claim, run `bash $SST3/sst3-code-search.sh '<pattern>' <lang>` (literal identifier when embeddings unavailable). Record every query + target + count + one spot-check file:line in the draft's References section. Skip if topic is semantic / non-code / one of the 12 subagent-only moments. **Citation freshness (AC 3.4):** for every `<doc-file>:<line-N>` reference in the draft, run `grep -n '<key term>' <file>` and confirm the content at that line matches the cited topic; record the grep output in References. For every ordinal/count assertion (N of M files, N entries, X/Y fixtures), compute current N and M from raw source and record the verification command output.
1c. **Scope decomposition (AC 3.11):** (A) for each sub-issue MERGE, list the component scopes, confirm each has a destination AC, and preserve phase-ordering (1a/1b/1c) where sub-scopes have implicit sequencing; (B) for each sub-issue DROP, decompose the dropped sub-issue into its component scopes and confirm each is deferred-with-trigger or absorbed into a surviving AC; (C) for any multi-axis goal (SEO, architecture, compliance), decompose into BOTH axes as separate scope dimensions before writing ACs; (D) for any new knob/mechanism with no documented current-problem evidence, mark `JBGE-DEFERRED` with a trigger condition rather than scoping it as an AC.
2. Read `../templates/issue-template.md`. Follow the template EXACTLY. Every section is mandatory:
   - PREREQUISITE CHECKPOINT (6 principle checkboxes)
   - Problem/Goal
   - Acceptance Criteria (phased, phase-checkpoint notes between phases)
   - Engineering Requirements (Type Contracts, Schema Contracts, Config Traceability)
   - Quality Mantras (VERBATIM — see below)
   - Cleanup Requirements (Code Hygiene + File Housekeeping)
   - Context + References
2a. **Voice canonical-anchor pre-draft step (AC 2.5):** for issues with operator-voice content (`invoked_skill ∈ {blog, voice-doc-repo, consulting}`), compile the operator's canonical opening lines across all live channels and paste them verbatim into the draft front-matter as `canonical_voice_anchors`; any new prose must preserve the semantic frame of at least one anchor.
3. Write ALL before/after illustrations. Show what exists NOW vs what it will look like AFTER so we can compare after implementation. These are mandatory for every change — code, config, behaviour, UI. **Truth-table temporal axis (AC 5.5):** for any truth table in an AC, add a Temporal axis row (mid-flight | terminal) and enumerate all parameter modes (`full_history=True/False`, etc.) — a table without the mid-flight-vs-terminal axis silently assumes terminal-only.
3.5. **Author self-check block (AC 2.1) — MANDATORY before any subagent dispatch.** Run the full pre-dispatch self-check catalogue in `standards/stage-2/pre-dispatch-self-checks.md`: (a) placeholder sweep → zero hits; (b) scope-narrowing; (c) min-v1 sketch before research-driven extensions; (d) template-structure check; (e) JBGE per-AC necessity; (f) budget sanity (`wc -l` before codifying any line-count budget); (g) reuse-before-new (AP #10 at draft-time); (h) cross-repo state (`ls`/`grep`/`gh` against repo Y for every "existing X in repo Y" claim); (i) multi-option AC classification (evidence-supported→force / destructive-irreversible→operator-pre-consent / policy-level→defer); plus item (j) author-time source-read gate (Read cited file:line into Before blocks, `sed -n '<line>p'` verbatim quotes, re-grep symbols, re-execute carried predicates, read reuse targets' full body); plus shape-gated item (k) multi-root script file-availability (for a bootstrap/installer script resolving files from >1 root, establish the step's run-order position and identify which root — pre-clone staged / post-clone repo / operator-invoked staging dir — makes the file present at THAT step before writing the import path). Zero unresolved items before dispatch.
4a. **PRE-DISPATCH FIRST PASS — placeholder/stub grep sweep (Theme 5, #477 AC 2.1)**: BEFORE dispatching Layer-1/2 audit subagents (step 5), run `grep -E 'placeholder|stub|\bTODO\b|if needed|nice-to-have|could be' /tmp/issue_draft_<topic>_<date>.md`. For each hit: replace with concrete spec OR document explicit out-of-scope-with-rationale (no "we might do this later" wording). **Zero hits required before subagent dispatch.** Rationale: subagents that audit a draft full of placeholder/stub language will mirror the placeholders forward as "captured scope" instead of flagging them as gaps — the audit becomes a rubber-stamp. **Finding-to-AC traceability gate (AC 2.3):** (1) extract every Fn AND every Gn from the Stage-1 research file; build a two-column table [finding_id | planned_AC_id]; every row must carry an AC number or explicit `out-of-scope: <rationale>` — gaps with neither block dispatch; (2) walk every imperative phrase ('AVOID', 'MANDATORY', 'required', 'must') in every finding and bind each to a specific AC at sub-element level, not headline level; (3) for every operator-supplied artifact name referenced in a draft AC, run `grep -rl <name>` in the repo — zero hits → AskUserQuestion before writing the AC; (4) if Stage-1 §3 uses unverified-assumption phrasing ('presumably', 'assumed', 'expected'), flag each as an open question to resolve in-draft, not carry forward.
4b. **POST-CORRECTIONS SECOND PASS — same grep sweep (Theme 5, #477 AC 2.2)**: AFTER Layer-2 corrections applied + amendments integrated (step 7), re-run the same grep sweep. **Zero hits required before declaring Stage 2 complete.** Rationale: Layer-2 corrections are inserted via copy-paste from subagent recommendations, which can re-introduce "could be / if needed / nice-to-have" wording even after Pass 1 was clean. Record hit→fix counts in the per-stage feedback file (`SST3-metrics/leader-feedback/feedback-<repo>-<issue>.md` Stage 2 `worked` field).
4c. Quality mantras — include VERBATIM in the issue scope (copy exactly, do not paraphrase):
   - No inefficiencies, fix optimisation opportunities
   - Reliable and robust (not prone to breakage or failing)
   - Dedupe duplicate codes
   - No bottlenecks
   - Runs super fast and safe
   - No memory leaks using preventions
   - Follows STANDARDS.md
   - No false positives — everything real gets fixed
   - No such thing as high priority or low priority — all must be fixed
<!-- Gate, not audit — see AP #23 delimiter (curated trigger list verifies named conditions; comprehensive-walk template does not apply). -->
4d. **Mirror-lane mapping (#460 Phase 8 W5 — AP #9 single-source-edits enforcement)**: **Read `SST3/drift-manifest.json` FIRST (AC 2.6)** — generate the vendored / unmirrored / no-manifest table by parsing the manifest, not by guessing from file paths. For any mirror-alignment draft, before writing the D.1.1 pattern-rewrite map, run `git log --all -S "<term>"` for every term and record the commit count; drop zero-history terms. Cross-check the final phase-A scrub list against Stage-1 §3 PUBLIC_SAFE classifications in a separate scope-exclusions pass. When the draft touches CLAUDE.md (template lane A) OR any file tracked in `SST3/drift-manifest.json:vendored_files` (mirror lane B), include a "Mirror Lanes Traversed" subsection in the draft listing the canonical edit locus + the consumer files that will be auto-propagated. Trigger conditions: SST3 section edits → Lane A; new/edited canonical script → Lane B; new GitHub Actions workflow with cross-repo applicability → Lane B. The Stage 4 implementation MUST run BOTH lanes' validators before Gate 1.
4e. **AC verifiability sweep (Theme 10, #477 AC 2.9)**: before dispatching the layer-1 swarm (step 5), the author reads each AC checkbox and confirms a falsifiable verification command/method exists (binary pass/fail). Rule lives in STANDARDS.md "Workflow Validation Gate" → "AC Verifiability — pre-Stage-3 sub-gate" + issue-template.md "AC Verifiability Gate" subsection. ACs without falsifiable verification get rewritten OR moved to "Items Not Fixed" with rationale BEFORE subagent dispatch — do NOT pass unfalsifiable ACs into the swarm. Pre-swarm rewrites are cheaper than post-swarm rewrites because subagents anchor on whatever scope they receive. **Two required sub-angles (AC 2.2):** (a) **Falsifiability** — for each verify command, mentally simulate a deliberate FAIL state; if the command passes in BOTH the pass and fail states, flag it as toothless and rewrite; (b) **Discriminability** — reject bare-substring greps where an exact-match pattern is available; reject narrative phrases ('output reports', 'reads', 'expects', 'appears to', 'should show') as verify text. Author-time preconditions: (c) when a script is named as a verify command, confirm it has an entry-point (`__main__` / argparse / shebang + exit-code contract) before writing it in; (d) when a gate is named as a verify command, confirm the gate's scope rules (exempt-path prefixes, mode skip matrix) admit the file under test; (e) any AC whose only verify is a live external web service or launch-day manual action must ALSO carry a CI-side local gate (parse / grep / exit-code). **`command-resolves` sub-check (AC 3.1):** for every Verification clause, (a) dry-run the named binary/path/flag from the target repo's worktree root; (b) replace `grep -P` with `grep -E`; (c) classify vacuous-truth patterns (`all()`/`any()` over empty iterables, Promise without await, grep matching comment-only lines). **AC design-quality checks (AC 4.5):** (1) dry-run each AC verification command against the current PRE-FIX tree and confirm it returns the FAIL value (non-zero exit / the pre-impl count); a verify that passes pre-fix is vacuous and MUST be re-scoped (line-range / exact-pattern / call-site anchor) before dispatch — **mechanise this dry-run with `python3 $SST3/check-ac-discriminability.py <draft.md> --tree-root <repo-root>` (dotfiles#528 AC 6.4): it flags grep/wc/`test` verifies that already pass against the pre-fix tree (exit 1 on any vacuous one); narrative / live-web / script-exec verifies are SKIPPED and remain this manual author step**; run `find tests/ -type d` and verify all cited paths exist; (2) for each AC, ask whether the machine-enforcement gate alone is sufficient to prevent the risk — split any AC containing operator-runtime gates into runtime-independent + runtime-dependent sub-ACs; (3) after any phase-rewriting implementation commit, identify every earlier AC whose satisfying text lived in that block and re-run its verification command (block-rewrite regression check); (4) before posting the Gate-1 close-out checkpoint, re-run each AC being marked complete — do not rely on in-session memory of test results.
5. Author + run the draft-check swarm via the Workflow tool (DEFAULT); layer-1 subtasks check draft scope against research findings. Full coverage — every finding must map to a scope item. **Layer-1 angles (AC 2.4 — catalogue `standards/stage-2/l1-l2-prompt-angles.md`):** add an **implementation-correctness** axis (for every AC, verify the verb-and-`file:line` against current source; flag any AC whose change description contradicts the actual source state) and a **carve-out-respect** angle (for each Stage-1 Hard NO / deferred / follow-up carve-out, confirm the draft does not re-absorb it).
6. Layer-2 subtasks in the same Workflow (DIFFERENT angle, per AP#14c) check for gaps, overengineering, and false positives. (For a trivial single-angle draft-check, Agent/Task is the documented fallback.) **Layer-2 angles (AC 2.4):** require reading the EXISTING code at the relevant `file:line` BEFORE evaluating any backwards-compatibility, semantic-equivalence, or literal-accuracy claim against source; plus optional shape-gated angles (admin-context PowerShell `HKCU`-write grep; service-name literals vs documented install output; `git log -S '<primitive>'` before any new concurrency primitive) and a marker-substring enumeration sweep for any YAML config an AC touches. Full catalogue: `standards/stage-2/l1-l2-prompt-angles.md`.
7. Everything needs to be fixed. No excuses, no deferring. No "high priority" or "low priority". ALL must be fixed unless confirmed false positive (document WHY with evidence). **(AC 3.4)** After any count correction, grep the draft for the OLD value and confirm zero hits — and the main agent re-runs the enumeration ITSELF from source; never accept a subagent's CORRECTED number without re-deriving it from source (dotfiles#528 AC 6.5: re-derive from source, not from the subagent's revised figure). **(AC 3.3)** When upgrading a verification predicate class (e.g. grep-c → per-token loop), audit ALL sibling ACs in the same phase for the same predicate class and apply the upgrade uniformly.
7.5. **Dependency matrix (AC 2.8):** for any draft with phased dependencies, author an explicit "Phase X → Phase Y gating matrix" subsection as a table listing every AC N.x with its blocking predecessor AC M.x. For planning issues that decompose into sub-issues, encode dependency markers explicitly in each sub-issue description: `[blocked-by: #N]` for hard sequential dependencies and `[BLOCKS: #N, #M]` for upstream gates. Skip-clean if the draft has only one phase.
8. Present `/tmp` file path to user for review.

**Skill-canonical at draft-write time (N32 Double-Guardrail)**: while authoring the draft, walk every `## ` and `### ` heading of the invoked-skill canonical (per Stage 1 metadata `skill_canonical_files`) and verify the draft does not violate any rule in any section. Use the comprehensive-walk template per STANDARDS.md "Skill-Canonical Audit Template (Comprehensive Walk)". This is the author-time counterpart to Stage 3's subagent verification. Do NOT shortcut to "verify A, B, C" curated examples — that's the recall-bounded failure mode documented in ANTI-PATTERNS.md AP #23.

**DO**: Follow the template verbatim. Include every mandatory section. Show before/after for every change.
**DON'T**: Create the GitHub issue. Skip sections. Paraphrase quality mantras. Defer fixes.

**Per-stage feedback (Layer C; per STANDARDS.md §Per-Stage Feedback Capture)**: write the Stage 2 block to the per-issue feedback file before declaring complete (10 fields per STANDARDS.md).

**SIGN-OFF**: When done, tell the user: "Stage 2 (Issue Draft) complete. Next: `/Leader 3` (Sanity Check + GitHub Issue Creation)."

---

## Stage 3 — Sanity Check + GitHub Issue Creation

**Stage 3 mandatory-reading directive (#498 Phase 4 AC 4.8)**: run `bash $SST3/load-stage-rules.sh 3` to load the Stage-3-tagged subset (skill-canonical compliance / scope-vs-audit / raw-tool counter-query rules). Replaces full-canon read at Stage 3 entry.


The draft exists. Now verify it is correct before it becomes the contract.

**Process**:
1. Read the `/tmp` issue draft from Stage 2. Read `graph_applicable` from the Stage 1 research file — do NOT re-classify independently (TB-2 single-declaration-carries-forward). **Binary grep gate (AC 3.5 + AC 7.2):** `grep -c '## Triple-Check Gate' /tmp/issue_draft*.md` and `grep -c '## Solo Assignment (SST3 Automated)' /tmp/issue_draft*.md` — either count == 0 → BLOCK issue creation. **ALSO** `/usr/bin/grep -c '## Chat Reconciliation' /tmp/issue_draft*.md` must be `>= 1` (the verifier-led reconciliation report from step 2a cannot be silently skipped) — `== 0` → BLOCK issue creation.
2. Author + run the sanity-check swarm via the Workflow tool (DEFAULT; minimum 3 subtasks, scale up; Agent/Task = the documented fallback only for a trivial single-angle check). **Multi-skill dispatch (AP #23)**: when invoked-skill canonical token cost exceeds ~5K (e.g. 3+ skills × deep canonicals), split the skill-canonical compliance angle into multiple subagents — one per canonical file — so each comprehensive-walk stays within its context budget. This is orchestrator-side guidance for the Leader; the per-subagent template (what each subagent receives verbatim) lives in STANDARDS.md "Skill-Canonical Audit Template (Comprehensive Walk)". **Dispatch preamble (AC 3.13):** attach the parked Stage-2 feedback file (or its Stage-2 section from `SST3-metrics/leader-feedback/feedback-<repo>-<issue>.md`) to the subagent scope snippet so subagents enter Stage-3 knowing what Stage-2 already resolved — do NOT re-raise findings Stage-2 already closed. Each verifies independently:
   - **Scope vs Audit**: 100% of research findings captured? Nothing missing? No gaps?
   - **No overengineering**: Scoped items that solve problems that don't exist? Remove them.
   - **Named-scope fidelity (AC 3.2)**: does any AC apply to a BROADER set than the operator literally named in the research restatement / `## Agreements Log`? Flag any superset (e.g. a named `MB100/MBS100` set silently widened to "all strategies" / "uniform"). The enumeration IS the scope — see STANDARDS.md "Named-Entity Scope — Don't Broaden a Named Set".
   - _(The former "Chat history check" + "Opposite-scoping check" subagent bullets are REMOVED — they were structurally vacuous because subagents never receive the conversation. They are replaced by the MAIN-AGENT-owned verifier-led step 2a below.)_
   - **Negative-polarity sweep (AC 5.6)**: for any AC that changes a config value, contractual term, or narrative framing, grep the full doc tree for the SUPERSEDED token (old value / old issue-number / old wording) and assert zero unexpected hits outside intentional historical comment blocks. A recalibration that updates the one site you noticed but leaves the old value load-bearing elsewhere is the AP #9 single-source failure.
   - **Producer-surface enumeration (AP #30 — dotfiles#528 AC 6.1)**: for any AC that changes a value crossing a process/serialisation boundary (HTTP body, websocket/SSE frame, cache key/field, DB column, emitted log/metric), enumerate EVERY producer of that value by CONCEPT ("what else emits this fact?") — NOT by shared token — and confirm each paired emit-surface is in scope or deliberately exempt. A token-blind producer pair (e.g. an SSE push beside a Redis `HSET`) escapes both the AP #24 literal-marker grep and the AP #14e pattern-class sweep; this is the manual angle that catches it. Skip-clean if the issue changes no cross-boundary value. See ANTI-PATTERNS.md AP #30.
   - **Dead/obsolete/legacy code**: Did the audit find cleanup targets? Are they in the issue? Structural layer: `bash $SST3/sst3-code-large.sh 200 <lang>` + `bash $SST3/sst3-code-impact.sh <base-branch>` on the area the Issue edits, when graph available and fresh (pre-query gate per STANDARDS.md "Structural Code Queries"). Surfaces dead-code / wide-blast-radius candidates that should be in scope. Subagent still verifies intent (intentional duplication vs accidental). **Raw-tool counter-query (#447 Phase 5 — Stage 3 sanity check)**: for any structural scope item the wrapper-lane surfaced (callers count, large-fn list, dead-code candidates, blast-radius), dispatch ONE subagent to run the equivalent raw tool (grep / ast-grep direct / find) on the same target. Compute delta. **Delta >50% wrapper/raw discrepancy = BLOCK Issue creation** until reconciled — either the wrapper missed instances (recall drift, file a separate fix Issue first) or the raw tool double-counted (false positive in raw, document and proceed). Record the counter-query + delta + reconciliation in the draft's References section.
   - **All scope in issue BODY**: Nothing in comments. Everything in the body.
   - **Skill-canonical compliance** (Double-Guardrail Principle — N32): subagent uses the comprehensive-walk template per STANDARDS.md "Skill-Canonical Audit Template (Comprehensive Walk)". Reads the invoked-skill canonical file(s) per Stage 1 metadata `invoked_skill` + `skill_canonical_files`, walks EVERY `## ` and `### ` heading, returns per-section [PASS|FAIL|N/A] with file:line evidence tagged by `source_file`. RESULT block extends with `canonicals_walked` + `section_failures` + `fallback_applied`. Do NOT pre-filter by which rules "look relevant". See ANTI-PATTERNS.md AP #23 for the curator-bounded-recall failure mode this template prevents. **Twist sub-prompt (#484 V2.3)**: when `invoked_skill ∈ {blog, voice-doc-repo}` AND the draft contains AI-integrated operator-supplied prose, the comprehensive-walk MUST additionally run the STANDARDS.md "Polish vs Twist (Semantic Frame Preservation)" TWIST-forbidden checklist against each polished hunk vs its operator-supplied source (qualifier-add / comparison→verdict e.g. `costs vs value`→`costs outweigh value` / hedge-drop / imposed analytical lens), returning per-hunk PASS/FAIL — the semantic-frame half of skill-canonical compliance the heading-walk alone cannot catch (AP #25; subagent-only, no programmatic detector possible).
   - **Mirror-lane mapping completeness** (#460 Phase 8 W5 — AP #9): subagent verifies that any draft AC touching CLAUDE.md or SST3-canonical code includes its "Mirror Lanes Traversed" subsection (per Stage 2 step 4a) AND that the propagation tooling (`propagate-template.py` for Lane A, `propagate-mirrors.py` for Lane B) is named explicitly in the AC's verification step. Drafts that edit canonical-mirrored surfaces without naming the lane fail this angle.
   - **Verify-command execution (AC 3.1)**: EXECUTE every AC verify command against live source; report PASS/FAIL per command; flag tautological verifiers — ones an implementer could satisfy while the defect still ships.
   - **Expected Behavior ↔ AC internal consistency (AC 3.3)**: for every bullet in Expected Behavior, identify the AC that would fail if the behavior is absent; flag any EB bullet with no AC binding. **Deferred-Feature Prose Coherence (AC 3.3)**: grep the draft for every symbol/function/feature name appearing in any DECISION CALLOUT deferral block; confirm every occurrence outside that block is intentional and documented.
   - **AC checkbox walk (AC 3.5 L1-A)**: walk every ``^- \[ \] \*\*AC`` checkbox sequentially, report the count, and compare against the Stage-2-declared count in the Triple-Check Gate.
   - **Filesystem-state verification (AC 3.7)**: walk every "X exists / X does not exist / X is at path Y / X has properties Z" claim in the draft; run `ls` / `find` / `git ls-tree <branch> <path>` / `grep -rln` against the actual repo at audit time; report PASS/FAIL per claim. For relocation issues, grep the ENTIRE `vendored_files` set for the moved path literal (not just draft-named files). For drift-manifest consumers, check `git ls-tree` on every relevant non-main branch for false-absent path claims. For operator-facing manual procedures, include a hardware-prerequisite sweep (what physical tools/keys/programmers must be on hand before each phase).
   - **Citation verification (AC 3.4 — conditional, >5 source line refs)**: one subagent exclusively grep-verifies each cited file:line; pass/fail per ref, no analysis.
2a. **Chat Reconciliation (Verifier-Led) — MAIN-AGENT-OWNED, non-delegable (AC 7.1).** This replaces the old subagent "Chat history" + "Opposite-scoping" bullets, which were structurally vacuous (subagents never receive the conversation). Extract the operator's raw typed messages with `python3 $SST3/extract-chat-agreements.py --issue <N>` and read the research file's `## Agreements Log`. Dispatch a THREE-MODEL neutral verifier panel — one Haiku + one Sonnet + one Opus, IN PARALLEL via the Workflow tool's per-agent `model` override (model diversity = different blind spots; distinct from Ralph's SEQUENTIAL code-review) — each given ONLY those raw messages + the agreements log and the NEUTRAL prompt — they are NOT shown the issue/scope/diff (so they cannot be led into rubber-stamping): "From these messages, what did the operator ask for / agree to / rule out? Cite each to a message; do NOT speculate beyond what is stated." The MAIN AGENT then double-checks each independent interpretation against (a) its own understanding and (b) the drafted issue scope — divergence is the drift signal: in-issue-not-in-interpretation = `invented`; in-interpretation-not-in-issue = `dropped`; contradicts = `inverted`. POST the consolidated report (per-model reads + the `{invented, dropped, inverted}` divergence) as a `## Chat Reconciliation` section in the issue draft body, then PAUSE for explicit operator approval of that report BEFORE creating the issue (step 6). Honest limit: the binary gate (AC 7.2) only guarantees the report is PRESENT; the panel + operator catch truthfulness. Full doctrine + template: STANDARDS.md "Chat Reconciliation (Verifier-Led)".
3. Layer-2 subtasks in the same Workflow (DIFFERENT prompt, per AP#14c): cross-check layer 1 findings. Specifically verify no false positives and no false negatives in scope. (Agent/Task = the documented fallback only for a trivial single-angle check.) **Layer-2 standing categories (AC 3.6/3.8/3.12):** REJECTED-FINDING-REVISIT — re-walk every finding the Stage-2 main agent rejected; confirm the rejection still holds against the current draft. Stage-2-correction audit — explicitly walk every AC modified by Stage-2 Layer-2; correction-on-correction drift is the highest-probability residual error class. For each Layer-2 subagent declare: (a) angle job spec, (b) which Layer-1 outputs it reads, (c) whether it is merged with another angle and why — record merges as `L2-merge:[angle-A+angle-B]: <justification>`.
4. Each subagent returns a RESULT block. Main agent reviews ALL results. **Before acting on any L1/L2 BLOCKING finding (AC 3.6):** (1) run the same raw query in main-agent context to source-verify the finding; (2) for wrong-identifier replacements, confirm the replacement is correct-by-intent (architecture alignment), not just correct-by-existence; (3) for FAIL verdicts touching another repo's issue closure, classify as a known over-scope class and demote to low-priority. **Cross-stage contradiction (AC 3.13):** when Stage-2 and Stage-3 subagents reach incompatible conclusions on the same factual question, the main agent reads the source file directly and cites the literal text as the resolution — NEVER resolve by recency bias (a newer subagent finding ≠ authoritative finding).
5. If ANY subagent finds a gap: fix the draft. Do NOT create the issue with known gaps.
6. Once ALL subagents agree the draft is complete and accurate AND the operator has approved the `## Chat Reconciliation` report (step 2a): create the GitHub issue using the verified draft verbatim.
7. Report the issue URL to the user.
8. **CONTEXT CHECK REMINDER**: After reporting the issue URL, tell the user their current context usage percentage and advise: "If remaining is near ~50% (≈500K of 1M used), run `/handover` + compact before invoking `/Leader 4` — Stage 4 re-invokes `/SST3-solo` which reloads STANDARDS.md + CLAUDE.md + WORKFLOW.md + the Issue, so a compact here loses nothing and gives maximum room for implementation."

**DO**: Investigate when subagents disagree. Fix gaps before creating the issue. Verify against chat history. Remind user about context before Stage 4.
**DON'T**: Rubber-stamp. Pick the convenient answer when subagents conflict. Put scope in comments.

**Per-stage feedback (Layer C; per STANDARDS.md §Per-Stage Feedback Capture)**: write the Stage 3 block to the per-issue feedback file before declaring complete (10 fields per STANDARDS.md).

**SIGN-OFF**: When done, tell the user: "Stage 3 (Sanity Check) complete. Issue created: [URL]. Context usage: X%. If remaining is near ~50%, run `/handover` + compact before next step. Next: `/Leader 4` (Implement — re-invokes /SST3-solo)."

---

## Stage 4 — Implement (includes Verification Loop + Merge + User-Review-Checklist)

**Stage 4 mandatory-reading directive (#498 Phase 4 AC 4.8)**: run `bash $SST3/load-stage-rules.sh 4` to load the Stage-4-tagged subset PLUS every stage-4/*.md extract file (verification-loop, ralph-review, the gate files, three-tier-testing, and the rest — the loader's glob is the authority on the set, deliberately not restated as a count or a full enumeration here because both drift as files are added; the prior hardcoded count of 9 had gone stale when three files landed under #516). The loader emits both the in-canon Stage-4-tagged sections AND the extract files in a single concatenated stream.


The issue is created and verified. Now implement it. Do NOT stop to ask permission. On long / multi-phase work, when remaining nears ~50% (~500K of 1M) auto-run `/handover` + compact + CONTINUE; never operate below 50% remaining (near-completion exemption: ~2-3 turns from done at ~51% → finish).

**When-needed (NOT every run):** for goal / discovery / analysis / optimization or iterate-until-met work (the try → fail → tweak → retry shape behind auto_pb #1610 / #1620), invoke `/goal-loop` — it wraps native `/goal` to loop until every acceptance gate passes or an honest NO-GO is recorded. It COMPOSES within this stage; it is not a mandatory step.

Stage 4 has three sequential gates after the implementation phases complete: Gate 1 Verification Loop, Gate 2 Merge, Gate 3 User-Review-Checklist. Ralph Review runs inside the implementation loop (step 7) BEFORE Gate 1.

**Process**:
1. Invoke `/SST3-solo` FIRST — this reloads STANDARDS.md, CLAUDE.md, and WORKFLOW.md. Mandatory even if already loaded. After a compact, these will be gone. This step ensures the agent is fully armed before touching code. **(AC 4.9)** After installing a new MCP server or external dependency at any Stage 4 checkpoint, call `TOOL_LIST` (or the equivalent catalog endpoint) and verify the tool catalog matches the Issue's claimed endpoint list before proceeding to Phase 1. **Stage-N-entry write-gate (AC 4.4):** confirm the `## Stage N-1` block exists in the per-issue feedback file before dispatching Stage N work (or set `reconstructed_stages: true` with rationale if preceding stages pre-dated the mechanism). **Stub-first (dotfiles#528 AC 5.2):** at the FIRST phase commit the feedback file may be a fresh template STUB (copy `templates/leader-feedback-template.md`, frontmatter filled, field values still `<to-be-filled>`) — the commit-msg presence hook is LENIENT on those placeholders so it does not block the first commit; the STRICT close-gate (Stage-5 completeness check) still rejects placeholders, so populate every field before Stage-5 sign-off.
2. Read the GitHub issue line-by-line. Not skim — READ.
2.5. **Enter an isolated worktree BEFORE any code edit (dotfiles#488 Fix-A — per the CLAUDE.md "Branch Safety (CRITICAL — DO NOT VIOLATE)" anchor)**: call the `EnterWorktree` tool (name it `solo/issue-{N}-{desc}`) instead of a bare `git checkout -b solo/...` in the shared clone. A clone has one HEAD/index; a concurrent agent's branch-create otherwise moves your HEAD and muddles implementations. The authoritative trigger is the CLAUDE.md Branch-Safety section (the `EnterWorktree` tool only activates from a user/CLAUDE.md/memory directive — this step REFERENCES that anchor, it does not replace it). `ExitWorktree action:keep` until Gate-2's push is confirmed landed, then `action:remove`. **(A) Worktree dep setup (AC 4.2):** immediately after EnterWorktree, run `scripts/setup-worktree-deps.sh` (or manually symlink `.venv` and `node_modules` to the parent clone's equivalents) to prevent pre-commit hook failures on the first commit. All Edit/Write/Bash file operations inside the worktree MUST use worktree-absolute paths — canonical-clone absolute paths silently no-op when CWD is in the worktree. **(B) Pre-commit formatter cascade prevention (AC 4.2):** run the formatter on each file immediately after a large edit and before `git add`; stage ALL files intended for a commit before committing any of them, to prevent the pre-commit stash/rollback cycle; commit `.pre-commit-config.yaml` first when the edit set includes it; capture full `git commit` output and assert the SHA line appears before proceeding.
3. Implement every phase in the issue's Acceptance Criteria. Commit per file. Push after each phase. **Shape-Change Caller Sweep (AC 4.3):** before committing any change that renames a symbol, restructures a data shape, or modifies default behaviour, run `grep -rn <old_name|old_key|old_default> .` across the entire repo including tests/, docstrings, and comments; update every hit in the same commit. For a new filter/allowlist on field X, run `grep -rln "X=" tests/` before the Phase-1 commit to confirm no pre-existing test fixture depends on the unfiltered value. **API-model integration-test scope (AC 5.1):** when the Issue touches API request models, include `tests/integration/` in the scope of any new integration-test AC — a request-model change is a contract change a unit test alone does not exercise. **Wiring-contract Ralph timing (AC 5.4):** for Issues introducing wiring contracts (orchestrator + systemd, CLI + service), Ralph must run BEFORE Stage 5 — not deferred until context budget allows; a wiring contract unverified by Ralph is the class Stage 5 should not be the first to catch.
3.2. **Pre-Ralph self-check (AC 4.11):** run `grep -nE "except.*continue|except.*pass|except Exception:|\bfallback\b|>/dev/null" <new-files>` — each hit MUST have an associated log/metric emit or be explicitly documented as a design-approved silent path.
3.5. **Test-prod call coverage audit at each phase boundary (Theme 9, #477 AC 3.5)**: BEFORE closing phase N via the Layer 1 MCP close-out (step 3a), the implementer runs the call-seam grep for every new public callable / response-payload field / config key added in phase N. Commands: (a) `grep -rnE "from <new-module> import|<new-module>\.<new-callable>" tests/` for every new public function/method; (b) `grep -rnE "<new-field>" tests/` for every new response-payload field; (c) `grep -rnE "<new-config-key>" tests/` for every new config key. **Empty grep on a new public callable / field / config key = FAIL — test seam missing**. Either add the test seam in this phase's commits OR document the explicit no-test-needed rationale (rare; usually valid only for trivial dataclasses / pure-data exports). Skip-clean if phase work is doc-only or wiring-only with no new public surface. Cross-reference: STANDARDS.md "Test-Prod Call Coverage Discipline" + sonnet-review.md "Test-Prod Call Coverage (Theme 9)" section.
3a. **Layer 1 — Phase-boundary checkbox close-out (AP #20 Tier A, MANDATORY in execute mode only)**: BEFORE moving from phase N to phase N+1, run `mcp__github-checkbox__update_issue_checkbox(issue_number, checkbox_text, evidence)` for every completed Tier A Acceptance Criteria checkbox in phase N. (Plan mode = read-only per CLAUDE.md; MCP write tools only fire when the user has explicitly entered execute mode via `/Leader 4` or `/SST3-solo` invocation.) Interleaved with the phase commits, not batched. Evidence MUST use canonical patterns from `../reference/tool-selection-guide.md` Example 2: file:line / commit hash / command+output / subagent RESULT comment-id. If the tool is deferred: `ToolSearch(query="select:mcp__github-checkbox__update_issue_checkbox,mcp__github-checkbox__get_issue_checkboxes")` first, then invoke. Comment-only progress is a direct AP #20 violation. **No phase boundary may be crossed with a Tier A `[ ]` box behind it.** (Canonical rule: ANTI-PATTERNS.md AP #20; evidence patterns: `../reference/tool-selection-guide.md` Example 2.) Complemented by Gate 1 Layer 3 final-check below. **MCP Checkbox Close-Out Protocol (AC 4.8):** (1) copy checkbox text verbatim from a fresh `get_issue_checkboxes` call — never retype from memory (backtick-pairing failures are silent); (2) for multi-line AC text with nested sub-bullets, pass only the first line (everything before the first `- (` sub-bullet) to the MCP tool; (3) close each AC checkpoint at the moment the phase commit lands, NOT batched at Ralph PASS; (4) on a 401 MCP token error, post a one-line operator alert on the issue (`[MCP-TOKEN-REFRESH NEEDED]`) and proceed via `gh issue edit --body-file` for body updates — never silently skip. **(AC 5.5) WARNING:** `gh issue edit --body-file` OVERWRITES the entire issue body, clobbering MCP-set checkbox `[x]` state — before using it, re-fetch the CURRENT body (or the `get_issue_checkboxes` state) and preserve every `[x]` mark in the file you pass, or the MCP-ticked ACs silently revert to `[ ]`; (5) fast-track / mini-issues: the AP #20 evidence trail is mandatory regardless of issue size. **[SKIP]-branch contract (AC 4.9):** `[SKIP]` branches in sample-invocation scripts must emit the exact re-run command and current git SHA, so the operator need not cross-reference handoff comments. **New Public Surface Self-Check (AC 4.12):** for every new public callable introduced in this phase, (1) run `grep -rn <name> <source-dirs>` and record the callers count + one production file:line as AC evidence; (2) answer three questions — lifecycle cleanup needed? audit trail exists? HTTP/serialisation tested end-to-end? — and block phase close if any answer is No with no documented rationale.
4. Do NOT stop between phases to ask "shall I continue?" — the answer is yes. Proceed.
5. On long multi-phase work, auto-`/handover` + compact + CONTINUE when remaining nears ~50% (~500K of 1M) — compaction is a continuation, not an artificial break; never operate below 50% remaining.
6. Run the Verification Loop (Gate 1 below) after all phases are complete. **Audit-phase freshness (MANDATORY first step)**: before ANY audit / wiring / `callers_of` / `impact` query in the Verification Loop, run `bash $SST3/sst3-code-update.sh` unconditionally — your solo-branch edits rendered any session-start snapshot stale. Same rule applies at the Stage 5 Post-Implementation Review audit phase (see playbook "Audit-phase freshness" bullet under "Stage 4 — Implementation (Verification Loop)"). Skip only if doc-only diff (exempts all graph checks).
6.5. **PRE-RALPH READINESS GATE (AC 4.12 — before dispatching Haiku):** (1) call `mcp__github-checkbox__get_issue_checkboxes` and batch-tick all ACs whose deliverables are committed and verified; (2) pass the resulting verified count ("N/M checkboxes ticked — verified via get_issue_checkboxes") in the frozen-scope snippet given to each Ralph tier; (3) if any AC has pending operational evidence (queue job, multi-hour backtest, operator-runtime gate), mark it `[deferred-N/A: <boundary-named>]` and defer Ralph dispatch until the evidence window closes. Dispatching Ralph with unchecked completable ACs guarantees a Haiku restart from an AP #20 miss.
7. Run Ralph Review (Haiku → Sonnet → Opus) — all 3 tiers mandatory. Ralph Review is a CODE DELIVERY VERIFICATION pass — it confirms the issue's Acceptance Criteria were delivered. It is DISTINCT from Stage 5 adversarial audit. Ralph passing does NOT substitute for Stage 5 subagent swarm (TB-3 N36). Model-selection rationale for the three Ralph tiers (Haiku surface / Sonnet logic / Opus deep): see `docs/research/model-selection-haiku-4-5.md`. **Terminology note**: Ralph "tiers" (sequential Haiku → Sonnet → Opus, restart on fail) are distinct from Stage 5 subagent "layers" (parallel Layer 1 + Layer 2, different angles per AP #14). Ralph is code-delivery verification; Stage 5 layers are post-implementation adversarial audit. AP #20 "Layer 1/2/3" enforcement in this skill body (step 3a + pre-Gate-1 paragraph + Gate 1 bullet) is a THIRD separate use of "Layer" terminology — execution-time enforcement gates, not review-time subagent dispatches. **Tier 3 Opus raw-tool counter-query rule (#447 Phase 5)**: when ANY Tier 3 finding depends on `../scripts/sst3-code-*.sh` output (recall counts, large-fn lists, callers, blast-radius, untested-py results), Opus MUST dispatch ≥1 raw-tool counter-query subagent on the same target before signing PASS. The counter-query result + delta vs wrapper-lane output is recorded in the Tier 3 review comment. Skipping this when wrapper-lane output is load-bearing = Tier 3 FAIL.
8. On Ralph FAIL: fix → restart from Tier 1. On Ralph PASS: proceed to Gate 1.
9. Post checkpoint to issue comment when implementation is complete.

**Layer 2 — Pre-Gate-1 double-check (MANDATORY before entering the Verification Loop)**: if the tool is deferred, `ToolSearch(query="select:mcp__github-checkbox__get_issue_checkboxes,mcp__github-checkbox__update_issue_checkbox")` first. Then run `mcp__github-checkbox__get_issue_checkboxes` and confirm every phase-completed Tier A checkbox is already `[x]` with canonical evidence (file:line / commit hash / command+output / subagent RESULT comment-id per `../reference/tool-selection-guide.md` Example 2). If any Tier A box corresponds to completed work but is still `[ ]`, close it NOW via `update_issue_checkbox` with canonical evidence — do NOT defer to Gate 1. Gate 1 is the FINAL check, not the first; it must enter from a clean baseline. (Complements Layer 1 at phase-boundary above; Gate 1 below is Layer 3 final-check.)

**Gate-1/Gate-2 Mechanical Pre-flight (AC 4.7).** Gate-1 entry pre-flight (run before ANY Verification Loop check): (a) for every AC naming a file artifact, run `test -f <path>` — any missing path = FAIL before continuing; (b) run `wc -c <issue-body-file>` — if the body exceeds 120000 bytes, use `gh issue edit --body-file` for all body updates (the MCP github-checkbox tool hits the kernel single-arg ceiling above this threshold); (c) if any wrapper-lane script exits engine-missing, log `[WRAPPER-LANE unavailable: <reason>]` and cross-reference a tracking issue before continuing.

### Gate 1: Verification Loop

> Canonical: ../workflow/WORKFLOW.md "## Verification Loop" — run that loop here. The Leader-specific gates below extend it; the generic checks (all-checkboxes-evidence / Overengineering / Architecture reuse / Code duplication / Fallback policy / Wiring / Quality scan / Ralph 3-tier confirmation) live in the canonical and MUST NOT be restated here (Cut #3, AC 1.6).

- [ ] **Graph-backed diff audit (MANDATORY when graph available per STANDARDS.md "Structural Code Queries")**: read `graph_applicable` from Stage 1 research file — do NOT re-classify (TB-2 carry-forward). If `graph_applicable=false`, skip this checkbox and proceed; if `true`, run `bash $SST3/sst3-code-review.sh <default-branch>` on the diff — always wrapped in a subagent with an UPFRONT warning in the prompt: "Expect 4MB+ JSON output — pipe to file + use `jq`/`python` to slice. Do NOT try to read the full payload." (Round-5 overflow data: 7× across 3 rounds / 144K-11.7M chars.) Feed the returned per-file `impacted_callers` counts + untested-function list (+ any `review-error` records — engine failures are findings, not noise; #544) into the swarm scope. This determines swarm size + angles, NOT replaces subagents. Subagents still handle semantic / intent / cross-document angles per AP #19 12-moments carve-out. If `review` output surfaces high-`impacted_callers` (wide-blast-radius) files, PRE-ASSIGN Layer-1 subagent angles by impacted hub (one subagent per hub) rather than generic-angle coverage. For hybrid diffs (code + docs): run `review` on the full diff — markdown/YAML/JSON files yield no impact edges, and `review` output tolerates that. **Internal-consistency check (#447 Phase 5)**: if the `review` JSON's `impact` array is EMPTY but the diff is non-trivial (>5 functions touched per `sst3-code-impact.sh`), treat the wrapper output as SUSPECT — dispatch ONE raw-tool subagent (grep / ast-grep direct) to compute the impact independently, compare, and gate on the delta. An empty review JSON paired with a real diff is a known wrapper failure mode that the self-test gate cannot fully cover (engine-broken vs engine-missing on the orchestrator path).
- [ ] **Layer 3 — Checkbox-MCP coverage gate (AP #20 FINAL CHECK)**: per canonical definition in `../workflow/WORKFLOW.md` "Verification Loop" — **(0) Check if post-commit hook + GHA already auto-ticked Phase M Tier-A boxes** (#477 Phase 6 AC 6.6 — Theme 6): inspect `SST3-metrics/.tier-a-auto-tick/<issue#>-<phase>.json` and the Issue body's most-recent edits — if every Tier-A box for the just-completed phase is already `[x]` with a `PoW [<ac_id>]: ... (auto-ticked via tier-a-auto-tick.yml)` line, document in checkpoint comment ("Tier-A auto-tick processor closed N/N boxes for Phase M") and skip directly to step (4) re-verification. If no (hook unavailable / GHA disabled / sentinel never written / box-text drift caused matcher miss / network failure), proceed with manual MCP invocation in steps (1)-(3) as before. Manual MCP override remains the AP #20 fallback. Then: (1) `ToolSearch(query="select:mcp__github-checkbox__get_issue_checkboxes,mcp__github-checkbox__update_issue_checkbox")` if deferred; (2) run `get_issue_checkboxes` and list every Tier A box still `[ ]` that corresponds to completed work; (3) for each such box, invoke `update_issue_checkbox(issue_number, exact_checkbox_text, evidence)` with canonical evidence (file:line / commit hash / command+output / subagent RESULT comment-id per `../reference/tool-selection-guide.md` Example 2); (4) re-run `get_issue_checkboxes` and confirm every Tier A box is now `[x]`. Comment-only progress = FAIL. Tier B batched-closures applied here are acceptable per Phase 9 cadence (ANTI-PATTERNS.md AP #20). Do NOT duplicate the rule here — WORKFLOW.md is canonical; only the procedure is expanded.
- [ ] **Three-Tier test gate (Unit / Workflow / E2E)** — per WORKFLOW.md "Verification Loop" canonical tier checkboxes (#484 T4.3): BUILD = all 3 tiers' tests exist for the change; USE = all 3 RUN (fire & pass) every change (operator directive dotfiles#528 — supersedes scope-matched fire; E2E may be sized to a small backtest / execution+cleanup but still RUNS; the only non-run is a documented `structural-inapplicable: <reason>`). Record the tier-evidence line `tiers: U=.. W=.. E2E=.. | BUILD-evidence:<file:line per tier>` (defined once in STANDARDS.md). The project test suite ("no regressions") = the union of the checked-in Unit + Workflow + E2E tests, not any single tier. WORKFLOW.md is canonical — do NOT re-define the spec here.
- [ ] **AP #18 sample-invocation = the Workflow Tier of the Three-Tier test gate above** (#484 T4.3): the real-CLI sample invocation, ≥3-repo-shape + raw-tool counter-query, row-count + downstream-consumer + wrapper-vs-raw-delta verification, exit-0-insufficient, and explicit `call_args.kwargs[...]` mock-assertion rules are the Workflow-Tier USE clause. Canonical spec: WORKFLOW.md "Verification Loop" Workflow Tier checkbox + ANTI-PATTERNS.md AP #18 + STANDARDS.md "Three-Tier Testing Framework". Leader defers — do NOT duplicate the spec here.
- [ ] **VL-1/VL-2/VL-3/VL-4 sub-checks (AC 4.6)** — per WORKFLOW.md Verification Loop extended sub-checks. Config/infra shapes additionally require an actual `workflow_dispatch` or PR-trigger CI run (not just a construction proof).
<!-- Gate, not audit — comprehensive-walk template per AP #23 does not apply (verifies named conditions, not multi-section canonical). -->
- [ ] **Skill-canonical verification gate** (N32 Double-Guardrail — per Guardrails principle): run the invoked skill's own verification checks. Examples: `check-ai-writing-tells.py` exit 0 (voice / blog / CV / voice-doc-repo); eBay 21-field count grep + SMART test evidence; claude-api prompt-caching verify; project-specific pre-commit hooks green. Evidence: pass/fail + output in issue comment.
- [ ] **Mirror-lane verification (Lane A + Lane B)** — per WORKFLOW.md "Verification Loop" Mirror-lane verification rule (#460 Phase 8 W5): canonical authoritative; do NOT duplicate the spec here.
- [ ] **Mirror-lane Lane A + Lane B exit 0** — three separate invocations all exit 0 (per WORKFLOW.md L105 — `--dry-run` and `--validate` are mutually exclusive in propagate-mirrors.py argparse, so they must run as separate commands): (1) `python3 $SST3/propagate-template.py --all --dry-run` (Lane A), (2) `python3 $SST3/propagate-mirrors.py --dry-run` (Lane B drift-check), (3) `python3 $SST3/propagate-mirrors.py --validate` (Lane B manifest-validate). Skip-clean if no canonical-mirrored surface touched.
- [ ] **Doc-lane diff-trigger (#484 W6.1 — NOT graph-gated)**: if this Issue's diff touches `*.md` / frontmatter / docs, the doc-lane (`sst3-doc-lint` / `sst3-doc-links` / `sst3-doc-frontmatter`) MUST have run regardless of `graph_applicable` (a `graph_applicable=false` semantic/governance Issue is NOT exempt — its `*.md` diff still gets linted). Canonical rule + rationale: WORKFLOW.md Stage 1 "Doc-lane is diff-triggered, NOT graph-gated"; Leader defers — do NOT re-define here. Skip-clean only if the diff touches no doc file.

If ANY check fails: fix, re-run ALL checks. The loop exits only when every check passes.

### Gate 2: Merge (recursion-safe remote fast-forward — dotfiles#488 AC 1.3)

The pre-#488 block performed a shared-tree branch-switch + pull + local-merge + push to main — the exact shared-HEAD mutation this Issue fixes (a shared-clone branch-switch moves every concurrent agent's HEAD). It is replaced with a pinned remote merge that touches **no shared working tree**:

1. Ensure all work is committed (per-file commits) by exact pathspec. **(AC 4.7) (d)** Detect the default branch dynamically: `git remote show origin | sed -n 's/.*HEAD branch: //p'` — never hardcode `master`/`main`. **(e)** Before any rebase, predict conflicts: `comm -12 <(git diff --name-only HEAD | sort) <(git diff --name-only origin/$(git remote show origin | sed -n 's/.*HEAD branch: //p') | sort)` must return zero lines.
2. From the worktree: `git push origin <solo-branch>` (publishes the branch).
3. Server-side fast-forward of `origin/master`: `git push origin <solo-branch>:master`.
   - On non-fast-forward rejection (the documented transient pre-push race — `origin/master` advanced; post-fix-B there is no 289-row index to corrupt so this is an ordinary push race that self-heals): `git fetch origin master`, then **inside the worktree** `git rebase origin/master` (preserving BOTH sets of changes — Solo Branch Merge Safety), then retry step 3. Bounded ≤3 attempts. **NEVER** `--force` / `--force-with-lease`.
   - This gate runs ONLY `git push` / `git fetch` / `git rebase` inside the isolated worktree. It NEVER branch-switches, local-merges, or resets the shared main working tree (the chokepoint #488 removes).
4. `ExitWorktree action:keep` until the push is confirmed landed (`git ls-remote origin master` == solo tip), then `ExitWorktree action:remove`; `git push origin --delete <solo-branch>`; `git fetch --prune`.

### Gate 3: User Review Checklist

1. Read `../templates/user-review-checklist.md`. Use the TEMPLATE. Do NOT invent your own.
2. Fill in ALL 10 sections. None are optional:
   - Scope Verification
   - Context Checkpoint
   - Gap Analysis — No Excuses Gate
   - How It Works
   - Cross-Issue Impact
   - Discoverability Check
   - Fail Fast Audit
   - Modular Architecture Review
   - Post-Implementation Review Gate
   - Closure Gate
3. Post the completed checklist as a comment on the GitHub issue.
4. Report to user: what was merged, issue URL, checklist posted.

**DO**: Read the issue thoroughly. Implement all phases. Commit per file. Push frequently. Run Ralph Review. Run all 3 gates sequentially. Use the template files. Verify every step end-to-end. Keep going until done.
**DON'T**: Stop to ask permission between phases. Run below 50% remaining without auto-`/handover` + compact on long work. Skim the issue. Leave phases incomplete. Fire-and-forget commits without verifying push succeeded. Skip the Verification Loop. Make up your own checklist. Defer the merge. Merge without Ralph evidence.

**Per-stage feedback (Layer C; per STANDARDS.md §Per-Stage Feedback Capture)**: write the Stage 4 block to the per-issue feedback file before declaring complete. Stage 4 sub-structure: `worked` / `didnt` carry per-Ralph-tier sub-bullets; `friction` records `ralph_restarts: <N>` + per-tier outcomes.

**SIGN-OFF**: When done, tell the user: "Stage 4 (Implement) complete. All phases implemented, Verification Loop passed, Ralph Review passed, merged to main, user-review-checklist posted. Next: `/Leader 5` (Post-Implementation Review — 100% mandatory subagent swarm)."

---

## Stage 5 — Post-Implementation Review (Subagent Swarm)

**Stage 5 mandatory-reading directive (#498 Phase 4 AC 4.8)**: run `bash $SST3/load-stage-rules.sh 5` to load the Stage-5-tagged subset (adversarial-audit / completeness-gate / regression / wrapper-vs-raw delta / §3-deferral re-litigation rules). Replaces full-canon read at Stage 5 entry.


Subagents audit everything the main agent produced. Main agent produced the work; subagents verify it. Stage 5 is a POST-IMPLEMENTATION ADVERSARIAL AUDIT — what did the main agent miss? 100% mandatory — every `/Leader` invocation terminates here. It is DISTINCT from Ralph Review (which ran at Stage 4 and verified delivery). Ralph PASS does NOT mean Stage 5 is satisfied — different lens, different class of findings (TB-3 N36).

**Process**:
0. **Step 0 — scope-snippet re-derive (AC 5.5)**: re-derive every cited line range via `grep -n` at HEAD; record the working-tree SHA (`git rev-parse HEAD`); carry `repo_shape`, `documented_false_positives`, and the Tier-A/B deferred-OK list into the scope file. NEVER inherit line ranges or "file X doesn't exist" claims from a handover or prior-stage artefact unchecked (probe-before-assert, AP #29). Line numbers shift between stages; a stale cited range sends the swarm to the wrong place.
1. Read `graph_applicable` and `invoked_skill` from the Stage 1 research file — do NOT re-classify (TB-2 carry-forward). If `graph_applicable=true`, run `bash $SST3/sst3-code-update.sh` to catch any edits since session start (AP #19 freshness — stale snapshots cause false negatives). Author + run the post-implementation audit swarm via the Workflow tool (DEFAULT; scale to implementation size; Agent/Task = the documented fallback only for a trivial single-angle check). Each subtask covers ONE angle with a RESULT block. One of those angles is a graph-backed audit (NOT a replacement — one audit input among several) when graph available per STANDARDS.md "Structural Code Queries": `bash $SST3/sst3-code-review.sh <default-branch>` (use `main` or `master` per repo default) on the diff, wrapped in a subagent with an UPFRONT warning in the prompt: "Expect 4MB+ JSON output — pipe to file + use `jq`/`python` to slice. Do NOT try to read the full payload." Feed the returned per-file `impacted_callers` counts + untested-function list (+ any `review-error` records — engine failures are findings, not noise; #544) into a subagent prompt. If the `review` output surfaces high-`impacted_callers` (wide-blast-radius) files, PRE-ASSIGN Layer-1 subagent angles by impacted hub (one subagent per hub) rather than generic-angle coverage — hub-anchored angles catch regressions that generic angles miss. Subagent checks whether each flagged item is expected or a regression; main agent verifies against source. Additionally, one subagent angle MUST verify skill-canonical compliance per Double-Guardrail Principle (STANDARDS.md) — the angle uses the comprehensive-walk template per STANDARDS.md "Skill-Canonical Audit Template (Comprehensive Walk)", reads `invoked_skill` + `skill_canonical_files` from the Stage 1 research file, walks every `## ` and `### ` heading of each canonical file against the DELIVERED work (commits + diff), and returns per-section [PASS|FAIL|N/A] with file:line evidence tagged by `source_file`. RESULT block extends with `canonicals_walked` + `section_failures` + `fallback_applied`. Same verify-against-source discipline as the graph-backed audit. See ANTI-PATTERNS.md AP #23. **Twist sub-prompt (#484 V2.4)**: when `invoked_skill ∈ {blog, voice-doc-repo}` AND the delivered diff contains AI-integrated operator-supplied prose, this skill-canonical angle MUST additionally run the STANDARDS.md "Polish vs Twist (Semantic Frame Preservation)" TWIST-forbidden checklist against each delivered polished hunk vs its operator-supplied source (qualifier-add / comparison→verdict / hedge-drop / imposed lens), per-hunk PASS/FAIL — twisting operator-supplied content is an AP #25 regression the heading-walk alone cannot surface; subagent-only, no programmatic detector possible. **Swarm-size heuristic (AC 5.7)**: ≤5 files / ≤50 lines → 3-4 L1 + 1 L2; ≥10 files or a new product module → ≥6 L1. Dispatch as a single parallel batch unless angle B depends on angle A's output. Default tier per STANDARDS.md "Default Tier Assignment per Swarm Role".
   - **Phase-by-phase scope review**: every issue phase delivered? Every acceptance criterion met?
   - **Goal alignment**: does the implementation solve the stated problem? "Code written" is not "problem solved".
   - **Wiring check**: every new function called by existing code? Every config key read? Every code path handles nulls? No orphaned code? Documentation references correct (file paths, line numbers, URLs all resolve)? Cross-references between docs, configs, and code all connected? Nothing dangling.
   - **Reliability and robustness**: is the implementation reliable and robust (not prone to breakage or failing)? Does it run super fast and safe?
   - **Inefficiency scan**: unnecessary allocations, O(n^2) where O(n) works, redundant DB queries, repeated calculations?
   - **Dead code cleanup**: obsolete/legacy code removed? Commented-out code removed? No backwards-compatibility hacks?
   - **Optimisation opportunities**: faster, simpler, more memory-efficient alternatives?
   - **Deduplication**: copy-pasted code that should be a shared function?
   - **Bottleneck detection**: single points of failure? Blocking operations that should be async?
   - **Memory leak prevention**: unclosed connections, uncleared caches, growing collections, event listener leaks?
   - **STANDARDS.md compliance**: all standards followed? Verification loop checks pass?
   - **Issue body 100% complete**: every checkbox ticked with evidence?
   - **Checkbox-coverage audit (AP #20)**: Proof of Work entries in issue body match every `[x]` box (per STANDARDS.md "Governance Evidence Signal (Canonical)"); Tier A interleave verified against solo-branch `git log` (entry order vs commit order) per opus-review.md Governance Drift Audit (lines 23-28). Any Tier A batching against commit cadence = FAIL and must be flagged in Stage 5 findings.
   - **§3-Deferral Re-Litigation Angle (Theme 7, #477 AC 2.5) — CONDITIONAL**: dispatch this angle ONLY when the user-review-checklist comment posted at Gate 3 has a non-empty §3 "Items Not Fixed" subsection. Subagent reads the user-review-checklist comment, classifies each unchecked/deferred item per the 3-flag taxonomy in `templates/user-review-checklist.md` §3: (a) `[deferred-FP: <evidence>]` — confirmed false positive; (b) `[deferred-N/A: <rationale>]` OR no flag at all — feasible fix incorrectly over-deferred (the absence of any flag IS the trigger); (c) `[deferred-tracking-issue: <issue#>]` — intentional user-approved descope to a tracking issue. Only (a) and (c) acceptable; (b) triggers a Stage 5 fix phase per "Fix Everything" guardrail (no deferrals, no scope excuses). Skip-clean if §3 is empty or every item has a (a)/(c) flag. The agent's own §3 entries are subject to the same re-litigation discipline — no self-exemption (#498 F-15). **The user-review-checklist comment is the authoritative §3 location — NOT the Issue body (AC 5.12)** — read the posted Gate-3 comment, not a §3 the agent may have duplicated into the body. Cross-reference: opus-review.md Pre-Merge Precondition Check (~L122).
   - **Mirror-lane post-merge regression check (#460 Phase 8 W5)**: confirm both lanes (Lane A `propagate-template.py --all --dry-run` exit 0; Lane B `propagate-mirrors.py --validate` exit 0) STILL pass post-merge. If a consumer drifted between merge and Stage 5 (e.g. parallel work landed there), surface in findings.
   - **Production-state verification (AC 5.2) — MANDATORY L1 angle**: run a production-state verification — read the live ledger/log/DB/CI state and verify the stated goal is met post-deploy — NOT just "code-written" (probe-before-assert, AP #29). "The diff adds the row" is a hypothesis until the row is read from the live store. For doc/governance changes the analogue is: the rule is actually loaded/enforced (e.g. `load-stage-rules.sh` emits it, the hook fires), not merely present in a file.
   - **Sibling-pattern enumeration (AC 5.3)**: for any finding about function X or pattern P, enumerate every sibling file in the same module for the same defect class before returning PASS — use concept-based grep (extract the concept, grep its synonyms), not literal-pattern grep (AP #14e). A parked finding with no assigned angle owner is a dropped finding.
   - **Negative-polarity sweep (AC 5.6)**: for any AC that changes a config value, contractual term, or narrative framing, grep the full doc tree for the SUPERSEDED token (old value / old issue-number / old wording) and assert zero unexpected hits outside intentional historical comment blocks (AP #9).
   - **Writer→reader shape audit (AC 5.14) — conditional**: when the diff touches backend response shapes (JSON field type / dict structure / enum value set / HSET key), dispatch a mandatory L1 angle that locates every frontend consumer via grep and verifies typeof guards, React-invariant safety, and bidirectional type-tolerance across the deploy window. Skip-clean if no response shape changed.
1a. **Chat Reconciliation (Verifier-Led) at Stage 5 — MAIN-AGENT-OWNED, non-delegable (AC 8.1; Stage 5 previously had ZERO chat-history check).** Extract the operator's raw typed messages with `python3 $SST3/extract-chat-agreements.py --issue <N>` and read the research `## Agreements Log`. Dispatch the THREE-MODEL neutral verifier panel — one Haiku + one Sonnet + one Opus, IN PARALLEL via the Workflow tool's per-agent `model` override (model diversity = different blind spots; distinct from Ralph's SEQUENTIAL code-review) — each given ONLY those raw messages + the agreements log (NOT shown the issue/diff) and the neutral prompt "from these messages, what did the operator ask for / agree to / rule out?". The MAIN AGENT then double-checks each independent interpretation against BOTH (a) the DELIVERED diff/commits AND (b) the issue scope — because the issue itself can have drifted from chat during Stages 2-4, so auditing only against the issue is insufficient; divergence = `invented` (delivered / in-issue, never agreed) / `dropped` (agreed in chat, not delivered) / `inverted` (delivered the opposite of what was agreed). POST the mismatch report into the user-review-checklist `## Chat Reconciliation` section (AC 8.2), then PAUSE for explicit operator approval BEFORE the Stage-5 sign-off. Doctrine: STANDARDS.md "Chat Reconciliation (Verifier-Led)".
2. Layer-2 subtasks in the same Workflow (DIFFERENT angle, per AP#14c): cross-check layer 1. Specifically look for false negatives (problems layer 1 missed). **Raw-tools-only requirement (#447 Phase 5; outside-diff scope AC 5.10)**: at least ONE Layer-2 subagent angle MUST use RAW tools ONLY — direct grep / direct ast-grep / direct find / git log — and explicitly prohibit `sst3-*.sh` invocation. The L2 prompt MUST include: "Look OUTSIDE the diff file list — the wrapper-blind class is precisely files NOT in the diff that the change semantically invalidates." The L2 RESULT block MUST carry `wrapper_invoked: no` as the TOP LINE plus one concrete `single highest-impact fix` finding. This is the audit-time counter-balance to wrapper-lane recall drift: when Stage 4 Verification Loop, Ralph Tier 3, and Stage 5 Layer-1 all use the same wrapper, the swarm becomes blind to wrapper bugs. The raw-only Layer-2 angle is the failsafe.
3. Main agent reviews ALL RESULT blocks. Verify findings against source. **Verdict-conflict check (AC 5.11)**: any RESULT block with verdict=PASS AND a finding severity > INFORMATIONAL = re-prompt that subagent immediately ("your finding warrants a FAIL — confirm or refute") before accepting the PASS.
4. Fix ALL problems. No deferrals. No "low priority". All fixed unless confirmed false positive (document why with evidence).
5. If fixes were applied: run regression tests AFTER the fixes. This is mandatory — fixing code can introduce new regressions. Verify no regressions before declaring clean.
6. If fixes applied: commit, push, update the issue.
7. If no fixes needed: run regression tests anyway to confirm clean state.
7a.0. **MANDATORY pre-completeness /tmp sweep** (#469 Phase 4): run `bash $SST3/sweep-parked-feedback.sh <issue-number> [--repo <repo>]` BEFORE the existing 7a completeness gate. Exit 0 mandatory; on non-zero (BLOCK class hit), parked feedback exists in /tmp and MUST be applied to canonical (`SST3-metrics/leader-feedback/feedback-<repo>-<issue>.md`) before sign-off. NEVER auto-apply (branch-safety + stale-parked-drift risk per Stage 1 F6) — surface to operator with the suggested apply commands the script prints, then resume. WARN class (HANDOVER files) + INFO class (sst3-feedback-test-* tempdirs) are advisory and do NOT gate sign-off; apply WARN class at operator discretion.
7a.1. **MANDATORY task-close drain gate** (#493 Phase 2): run `bash $SST3/leader-stage5-drain-check.sh <issue-number> [--repo <repo>] [--waive-residue <class>:<reason>]` BETWEEN the sweep (7a.0) and the completeness check (7a). Exit 0 mandatory; on FAIL (exit 1), unwaived residue is present across one or more of D1-D6 (uncommitted task-touched files / self-created stash / self-opened worktree / un-pushed commits / unfinished propagation tail / **D6: the issue's dotfiles feedback file `SST3-metrics/leader-feedback/feedback-<repo>-<issue>.md` is not committed + pushed + synced to `origin/master`**). Feedback ALWAYS lives in dotfiles even when the work repo differs, so a consumer-repo session must STILL commit+push its dotfiles feedback — D6 enforces this cross-repo (the failure mode it closes: feedback files were sitting orphaned/un-aggregated because the work-repo drain never saw the dotfiles-side feedback). Either drain (commit, push, ExitWorktree remove, propagate-mirrors --apply) and re-run, OR pass an explicit `--waive-residue <class>:<reason>` flag per class to record the operator's deliberate exception in the JSON summary + an HTML-comment receipt on the Issue body. Exit 2 = usage error or `--repo` not in KNOWN_REPOS (fail-fast operator-typo guard). Layer B post-flight failsafe (`.github/workflows/stage5-completeness.yml`) replays the same gate server-side; both layers are mandatory.
7a. **MANDATORY pre-sign-off completeness gate** (#460 Phase 7 W4 Layer A): run `bash $SST3/leader-stage5-completeness-check.sh <issue-number>` BEFORE the sign-off comment is posted. Exit 0 mandatory; on FAIL, fix the missing invariant(s) and re-run. JSON summary captured in the Stage 5 checkpoint comment for audit trail. Layer B post-flight failsafe (#460 Phase 7b GitHub Actions workflow `stage5-completeness.yml`) replays the same gate server-side after the comment is posted — both are mandatory; neither replaces the other.
8. Report to user: findings, fixes, regression test results, final state. Record `last_updated` + `file_count` in the Stage 5 checkpoint comment — parity with Stage 1a audit trail (TC-10 S3). Reviewers need the same wrapper-lane status audit at Stage 5 entry as at Stage 1 entry.
8a. **MANDATORY closing-summary comment (dotfiles#528 AC 3.1 — "the final comment is always forgotten").** BEFORE the sign-off, post an agent-authored closing-summary comment to the Issue carrying the machine-detectable sentinel **`<!-- sst3-closing-summary -->`** on its own line. Contents: outcome (done / blocked + why), the Stage 4 + Stage 5 commit SHAs, the 7a completeness-check JSON, and the Ralph tier verdicts (H/S/O). This is the comment the closing agent otherwise forgets; C18 (step 8b) enforces its presence. (The sentinel is private-term-free → it ships to the public mirror verbatim; pinned `readonly` in the single bash reader `leader-stage5-completeness-check.sh`, hand-duplicated at the markdown sites.) NOTE: do NOT post it via `gh issue edit --body-file` (that clobbers the body + MCP checkbox state — AC 5.5); use `gh issue comment`.
8b. **Re-run the completeness gate WITH the closing-comment flag (C18 enforce pass).** Run `bash $SST3/leader-stage5-completeness-check.sh <issue-number> --expect-closing-comment` — the 7a run recorded C18 `skip` (pre-comment); this idempotent final pass enforces C18 (fail-closed) and now PASSes because step 8a posted the sentinel comment. Exit 0 mandatory. The Layer-B `stage5-completeness.yml` server-side replay enforces C18 identically (won't fire until the GHA monthly limit resets — the manual 8b run is the live gate meanwhile). Only after 8b passes → SIGN-OFF.

**DO**: Require specific findings from every subagent (file:line, evidence). Fix everything found. Run regression tests.
**DON'T**: Accept "looks fine" from subagents. Defer fixes. Skip regression tests. Fire-and-forget. Create new GitHub issues autonomously — propose Stage 5 findings as comments on the parent issue; creating new issues requires explicit user authorization in chat (AP #21, added #431 Phase 2b).

**Per-stage feedback (Layer C; per STANDARDS.md §Per-Stage Feedback Capture)**: write the Stage 5 block to the per-issue feedback file before declaring complete (10 fields per STANDARDS.md). After writing, update frontmatter `stages_logged` to include 5 + bump `last_updated`; aggregator regenerates index automatically on next commit.

**SIGN-OFF**: When done, tell the user: "Stage 5 (Post-Implementation Review) complete. All work audited, fixes applied, regression tests passed. Issue [URL] is done. Close the issue when satisfied."
