---
description: Leader Mode — run the operator-directed SST3 5-stage workflow (/Leader 1-5 = research, issue creation, triple-check, implementation, post-implementation review). Instructions are orders: execute exactly as written, do not skim, summarise, or skip stages.
---

# Leader Mode

Direct instructions from the human leader. NOT suggestions. Orders. Do not skim. Do not summarise. Do not skip steps. Execute exactly as written.

## Guardrails (Apply to ALL Stages)

**SST3 script path — resolve `$SST3` FIRST, in the same shell call**:

```bash
SST3=$( [ -f scripts/load-stage-rules.sh ] && echo scripts/ || echo "<your-dotfiles-clone>/scripts/" )
```

Then invoke every SST3 script as `bash $SST3/<script>.sh ...`. A bare `scripts/...` exits 127 from any consumer repo; a fixed `$HOME` path silently reads the MAIN dotfiles clone even from a dotfiles worktree, so canon edits in flight are not seen. The resolver picks the local canon when one exists and falls back otherwise. Confirm exit 0. **A 127 here is a path failure, not a missing inner engine.** If the fallback branch also 127s, derive the root from the installed command symlink: `dirname $(dirname $(readlink -f ~/.claude/commands))`.


- **Context budget**: Research phase <30% of context. On long work, when remaining nears ~50% (~500K of 1M) the agent AUTO-runs `/handover` + compact + continue; never operate below 50% remaining.
- **Post-compact recovery**: If context was compacted mid-stage, re-read this skill file's active `/Leader` stage section line-by-line + the handover file in full (if `~/handover/current-task.txt` is present) + CLAUDE.md + STANDARDS.md + STANDARDS.md §Per-Stage Feedback Capture (in-flight workflow telemetry) + the active Issue before continuing. Do NOT resume from memory — a pre-compact read does not survive a compact (memory is diluted; re-read, do not assume).
- **Branch safety**: NEVER switch branches. Commit and push to the CURRENT branch. Ask the user if you need something from main.
- **Commit discipline**: Commit per file. NEVER `git add -A` or `git add .`.
- **AP #16 Monitor**: Every command, script, test, commit, push — verify end-to-end. Check exit codes, tail logs, confirm side effects. "Started" is not "done".
- **Subagent RESULT block**: Every subagent prompt MUST require a fenced `## RESULT` block at the end of its response:
  ```
  ## RESULT
  mcp_graph_available: yes|no   (FIRST line when subagent discusses graph queries — AP #19)
  verdict: pass|fail|unknown
  files_touched: [list]
  findings: [{path, line, claim, evidence}]
  tee_log: <path or "none">
  scope_gaps: [list or "none"]
  evidence_command: <exact shell command>   (MANDATORY for any angle whose deliverable is a COUNT, INVENTORY, or BYTE-SIZE — AC 1.6)
  evidence_stdout: <verbatim output (or NOT_RUN if untestable)>
  ```
  Prose-only numbers are not verifiable — if you cannot paste the `evidence_command` + its `evidence_stdout`, state NOT_RUN (AC 1.6). files_read tagging (#555 Phase 4): a file inspected outside the subagent's primary angle is logged in files_touched with a `(cross-angle)` tag — never buried in scope_gaps — so the completeness critic counts it as covered. Multi-pass runs (#555 Phase 4): before dispatching pass 2 of a multi-pass subagent run, verify the pass-1 probe/output file exists with >N lines (hard state-capture barrier); label every RESULT number MEASURED (a command ran) vs INFERRED (derived without re-running); escape pgrep self-match via a character-class pattern.
- **Scope snippet**: When dispatching >=10 subagents, write a frozen scope snippet (<=2K tokens) to `/tmp/sst3-scope-<topic>.md` and pass the PATH to each subagent — not the full issue body. **Scope-snippet completeness checklist (AC 1.4)** — the frozen snippet MUST carry: [ ] **Structural decisions section**: cross-repo branch model, orchestration choices (for issues touching >=2 repos); [ ] **Verbatim operator constraints**: the 1-2 most load-bearing constraints embedded literally; [ ] **Known design limits appendix**: previously-documented out-of-scope items and why; [ ] **Binary decision gates**: any yes/no choices that block implementation, surfaced explicitly at the top; **(#555 Phase 3)** a scope expansion into a second language/stack/runtime MUST be written as a binary decision gate the operator ticks, never as a prose-only Scope note. If scope expands mid-swarm, write a `scope-expansion-delta` sub-block to the research file (what was added, why, date) — do NOT silently fold mid-stage scope changes into the main findings. **(#555 Phase 4)** any pre-research path/consumer claim not yet swarm-verified is labelled `UNVERIFIED-pending-swarm` inline, never stated as fact; when multiple angles may compute the same metric, pin the canonical measurement command VERBATIM in the snippet preamble so cross-angle numbers cannot fork on methodology.
- **Double-Guardrail Principle (N32)**: every `/Leader` invocation checks BOTH the cross-cutting SST3 canonical (STANDARDS.md + ANTI-PATTERNS.md + WORKFLOW.md + project CLAUDE.md) AND the invoked-skill's domain canonical (voice_rules, eBay rules, blog voice-guard, voice-doc-repo dual-lens, claude-api prompt caching, project CLAUDE.md rules). Stage 1 identifies the invoked skill and records `invoked_skill` + `skill_canonical_files` in the research file. Stage 3 adds a skill-canonical compliance subagent angle. Stage 4 Gate 1 + Stage 5 run the skill's own verification hooks. See STANDARDS.md "Double-Guardrail Principle".
- **Multi-layer discipline (AP #14)**: Minimum 2 layers of subagents. Layer 2 MUST use a DIFFERENT prompt/angle than layer 1. Main agent verifies EVERY finding against source before acting. Swarm recommends; main agent verifies; source decides.
- **Never Assume**: Read the actual source before drawing conclusions. Do not state file contents without reading. Do not assume variable names, API shapes, or function signatures from memory. When in doubt, read first, conclude after.
- **Fix Everything**: All problems found get fixed — no deferrals, no scope excuses, no language boundaries. Python, Rust, JS, SQL, YAML, shell — fix them all. Only valid skip: confirmed false positive (document why).
- **FACT-CHECK RULE**: Default position: doubt yourself. Unless a claim is 100% backed by evidence, it is NOT factual and MUST be treated as unverified. Do not trust your memory. Do not trust your own confidence. Memory can be diluted, summarised incorrectly, or outdated. A memory entry without reference links, evidence, or calculations proving how figures were derived is NOT a reliable source. If memory says X but you cannot find the file, line, command output, or calculation that proves X, then X is UNVERIFIED — go find the proof or retract the claim. When in doubt, doubt yourself — then go verify.
- **Governance Enforcement — Checkbox MCP (AP #20)**: at every Acceptance Criteria checkbox completion → invoke `mcp__github-checkbox__update_issue_checkbox(issue_number, checkbox_text, evidence)` with per-deliverable evidence. Comment-only progress is NOT a substitute. **Before invocation**: check for deferred-tool status and load the schema via `ToolSearch` per STANDARDS.md "MCP Tool Schema Loading" — mandatory, not optional. Canonical evidence-quality patterns: `../reference/tool-selection-guide.md` Example 2.

## Stage Selection

If the user provided a stage number as an argument (e.g. `/Leader 3`), go directly to that stage. Treat `/Leader 6` as a deprecated alias for `/Leader 5` — silently proceed to Stage 5. If no argument, present this menu and WAIT:

```
Leader Mode — pick a stage:

1. Research     — subagent swarm to research a topic, findings to /tmp
2. Issue Draft  — prepare /tmp issue from research, full quality sweep
3. Sanity Check — triple-check scope vs audit, then create GitHub issue
4. Implement    — /SST3-solo, implement all phases, Verification Loop, Ralph Review, merge, user-review-checklist
5. Post-Implementation Review — subagent swarm post-implementation deep audit (100% mandatory)
```

Which stage? (1-5)

---

## Stage 1 — Research (Subagent Swarm -> /tmp)

**Stage 1 mandatory-reading directive (#498 Phase 4 AC 4.8)**: run `bash $SST3/load-stage-rules.sh 1` to load the Stage-1-tagged canonical subset (research / subagent-swarm / pre-swarm SEED rules). The loader replaces the prior "read STANDARDS.md in full" directive; per-stage tagging emits ~28K bytes vs the full ~50K canonical. Always-load carve-out (privacy / voice / destructive-op) is included automatically. Non-zero exit = STOP; do not proceed on partial canon.

**Procedure (relocated — #555 AC 2.6)**: the full Stage-1 process (scale rule + steps 0–6 + DO/DON'T) lives in `standards/stage-1/research-swarm-procedure.md` and is EMITTED by the Stage-1 canon load above — follow it step-by-step from that emit; do NOT reconstruct it from memory. A non-zero loader exit means you have NO procedure: STOP.


**Per-stage feedback (Layer C; per STANDARDS.md §Per-Stage Feedback Capture)**: write the Stage 1 block to the per-issue feedback file before declaring complete. 10 fields (model / worked / didnt / why / improvement / improvement_status / evidence / friction / rule_self_caught / rule_user_caught), soft-cap guidance per STANDARDS.md, no forward-preference phrasing. Durability (#555 Phase 4): interim park = `~/handover/` (reboot-durable), never /tmp; write the block at EACH stage close, not one end-of-run batch.

**SIGN-OFF**: When done, tell the user: "Stage 1 (Research) complete. Next: `/Leader 2` (Issue Draft)."

---

## Stage 2 — Issue Draft (/tmp, NOT GitHub)

**Stage 2 mandatory-reading directive (#498 Phase 4 AC 4.8)**: run `bash $SST3/load-stage-rules.sh 2` to load the Stage-2-tagged subset (issue-template / acceptance-criteria / quality-mantras / AC-verifiability rules). Replaces full-canon read at Stage 2 entry. Non-zero exit = STOP; do not proceed on partial canon.


**Procedure (relocated — #555 AC 2.6)**: the full Stage-2 process (draft-first rule + steps 1–8 + skill-canonical walk + DO/DON'T) lives in `standards/stage-2/issue-draft-procedure.md` and is EMITTED by the Stage-2 canon load above — follow it step-by-step from that emit; do NOT reconstruct it from memory. A non-zero loader exit means you have NO procedure: STOP.


**Per-stage feedback (Layer C; per STANDARDS.md §Per-Stage Feedback Capture)**: write the Stage 2 block to the per-issue feedback file before declaring complete (10 fields per STANDARDS.md).

**SIGN-OFF**: When done, tell the user: "Stage 2 (Issue Draft) complete. Next: `/Leader 3` (Sanity Check + GitHub Issue Creation)."

---

## Stage 3 — Sanity Check + GitHub Issue Creation

**Stage 3 mandatory-reading directive (#498 Phase 4 AC 4.8)**: run `bash $SST3/load-stage-rules.sh 3` to load the Stage-3-tagged subset (skill-canonical compliance / scope-vs-audit / raw-tool counter-query rules). Replaces full-canon read at Stage 3 entry. Non-zero exit = STOP; do not proceed on partial canon.


**Procedure (relocated — #555 AC 2.6)**: the full Stage-3 process (steps 1–8 incl. verifier-led Chat Reconciliation + DO/DON'T) lives in `standards/stage-3/sanity-check-procedure.md` and is EMITTED by the Stage-3 canon load above — follow it step-by-step from that emit; do NOT reconstruct it from memory. A non-zero loader exit means you have NO procedure: STOP.


**Per-stage feedback (Layer C; per STANDARDS.md §Per-Stage Feedback Capture)**: write the Stage 3 block to the per-issue feedback file before declaring complete (10 fields per STANDARDS.md).

**SIGN-OFF**: When done, tell the user: "Stage 3 (Sanity Check) complete. Issue created: [URL]. Context usage: X%. If remaining is near ~50%, run `/handover` + compact before next step. Next: `/Leader 4` (Implement — re-invokes /SST3-solo)."

---

## Stage 4 — Implement (includes Verification Loop + Merge + User-Review-Checklist)

**Stage 4 mandatory-reading directive (#498 Phase 4 AC 4.8)**: run `bash $SST3/load-stage-rules.sh 4` to load the Stage-4-tagged subset PLUS every stage-4/*.md extract file (verification-loop, ralph-review, the gate files, three-tier-testing, and the rest — the loader's glob is the authority on the set, deliberately not restated as a count or a full enumeration here because both drift as files are added; the prior hardcoded count of 9 had gone stale when three files landed under #516). The loader emits both the in-canon Stage-4-tagged sections AND the extract files in a single concatenated stream. Non-zero exit = STOP; do not proceed on partial canon.


The issue is created and verified. Now implement it. Do NOT stop to ask permission. On long / multi-phase work, when remaining nears ~50% (~500K of 1M) auto-run `/handover` + compact + CONTINUE; never operate below 50% remaining (near-completion exemption: ~2-3 turns from done at ~51% → finish).

**When-needed (NOT every run):** for goal / discovery / analysis / optimization or iterate-until-met work (the try → fail → tweak → retry shape behind auto_pb #1610 / #1620), invoke `/goal-loop` — it wraps native `/goal` to loop until every acceptance gate passes or an honest NO-GO is recorded. It COMPOSES within this stage; it is not a mandatory step.

Stage 4 has three sequential gates after the implementation phases complete: Gate 1 Verification Loop, Gate 2 Merge, Gate 3 User-Review-Checklist. Ralph Review runs inside the implementation loop (step 7) BEFORE Gate 1.

**Process**:
1. Invoke `/SST3-solo` FIRST — this reloads STANDARDS.md, CLAUDE.md, and WORKFLOW.md. Mandatory even if already loaded. After a compact, these will be gone. This step ensures the agent is fully armed before touching code. **(AC 4.9)** After installing a new MCP server or external dependency at any Stage 4 checkpoint, call `TOOL_LIST` (or the equivalent catalog endpoint) and verify the tool catalog matches the Issue's claimed endpoint list before proceeding to Phase 1. **Stage-N-entry write-gate (AC 4.4):** confirm the `## Stage N-1` block exists in the per-issue feedback file before dispatching Stage N work (or set `reconstructed_stages: true` with rationale if preceding stages pre-dated the mechanism). **Stub-first (dotfiles#528 AC 5.2):** at the FIRST phase commit the feedback file may be a fresh template STUB (copy `templates/leader-feedback-template.md`, frontmatter filled, field values still `<to-be-filled>`) — the commit-msg presence hook is LENIENT on those placeholders so it does not block the first commit; the STRICT close-gate (Stage-5 completeness check) still rejects placeholders, so populate every field before Stage-5 sign-off. **Direct-entry metadata backfill (#555 Phase 3):** if this Issue entered directly at Stage 4 (Stages 1-3 skipped — no Stage-1 research file exists), write a one-line `graph_applicable` + `invoked_skill` + `skill_canonical_files` record to the Issue (or a stub /tmp research file) at Stage-4 entry, so Stage-5 gates that read those fields never silently re-derive or skip them. **Empirical-over-assumed-source (#555 Phase 4 — generalises the TOOL_LIST rule):** (1) config/env-keys derived from a sibling repo are verified against the RUNNING APP's own source, and lint-clean claims name the exact command/ruleset run; (2) a third-party library call embedded in an AC has its actual API/formatting semantics verified at write time (read source or probe), never left to the test tier; (3) simulate a chronic-red CI fully locally ONCE to enumerate the whole failure stack in one pass; (4) verify script exec bits survive Edit-tool edits.
2. Read the GitHub issue line-by-line. Not skim — READ. **Handover-count re-derivation gate (#555 Phase 3):** (1) re-derive any inherited "N known pre-existing failures" count by running the failing-test set against a clean, freshly-checked-out base-branch baseline (detached worktree); (2) re-run the FULL CI gate set verbatim before trusting a handover's "clean" claim; (3) for a completeness AC, the committed Stage-1/2 inventory is authoritative — a handover-survey re-derivation is a cross-check lead only; (4) source any private-data grep from the canonical IDENTITY list, never a tokenisation aid.
2.5. **Enter an isolated worktree BEFORE any code edit (dotfiles#488 Fix-A — per the CLAUDE.md "Branch Safety (CRITICAL — DO NOT VIOLATE)" anchor)**: call the `EnterWorktree` tool (name it `solo/issue-{N}-{desc}`) instead of a bare `git checkout -b solo/...` in the shared clone. A clone has one HEAD/index; a concurrent agent's branch-create otherwise moves your HEAD and muddles implementations. The authoritative trigger is the CLAUDE.md Branch-Safety section (the `EnterWorktree` tool only activates from a user/CLAUDE.md/memory directive — this step REFERENCES that anchor, it does not replace it). `ExitWorktree action:keep` until Gate-2's push is confirmed landed, then `action:remove`. **(A) Worktree dep setup (AC 4.2):** immediately after EnterWorktree, run `scripts/setup-worktree-deps.sh` (or manually symlink `.venv` and `node_modules` to the parent clone's equivalents) to prevent pre-commit hook failures on the first commit. All Edit/Write/Bash file operations inside the worktree MUST use worktree-absolute paths — canonical-clone absolute paths silently no-op when CWD is in the worktree. **(#555 Phase 3)** After EnterWorktree, positively assert `git rev-parse --show-toplevel` matches the intended worktree name BEFORE the first edit — absence-only checks are not enough — and never run `git stash` / `git stash pop` in the shared clone. Every Ralph-review / Stage-1/3/5 swarm prompt that runs shell on a solo branch carries a STEP-0 pre-flight: assert CWD's toplevel matches the target worktree before any check runs. **(B) Pre-commit formatter cascade prevention (AC 4.2):** run the formatter on each file immediately after a large edit and before `git add`; stage ALL files intended for a commit before committing any of them, to prevent the pre-commit stash/rollback cycle; commit `.pre-commit-config.yaml` first when the edit set includes it; capture full `git commit` output and assert the SHA line appears before proceeding. **(#555 Phase 3)** Assert each commit's SHA via `git log -1 --format=%H` compared against pre-commit HEAD — never by grepping hook stdout for a SHA-looking line; derive handover commit counts via `git rev-list --count <base>..HEAD`, never a hand-written list; before each per-concern commit, `git reset` (unstage all) then `git add` only that concern's files — a PRIOR aborted pre-commit's staged set otherwise bleeds into the next commit. Never chain `git stash pop` after a conditional `git stash push` — verify `git stash list` shows your message-stamped entry first, or use `cp` + `git show HEAD:<path>` for single-file checks instead; batch at most 2-3 hook-bearing commits per Bash call (or set timeout ≥ N×60s); after a killed commit, run `git status` and restore from the newest pre-commit stash patch before further edits. **(#555 Phase 4)** In a worktree, NEVER `prettier --write` (or formatter-equivalent) a pre-existing file that is not already formatter-clean on main — restore base + re-apply minimal hunks (a whole-file reformat is the whole-tree-churn hazard); dotfiles-script resolution uses the `$SST3` resolver (worktree depth breaks `../dotfiles` relative paths), and `.env` symlinking is owned by `scripts/setup-worktree-deps.sh` (#528).
3. Implement every phase in the issue's Acceptance Criteria. Commit per file. Push after each phase. **Shape-Change Caller Sweep (AC 4.3):** before committing any change that renames a symbol, restructures a data shape, or modifies default behaviour, run `grep -rn <old_name|old_key|old_default> .` across the entire repo including tests/, docstrings, and comments; update every hit in the same commit. For a new filter/allowlist on field X, run `grep -rln "X=" tests/` before the Phase-1 commit to confirm no pre-existing test fixture depends on the unfiltered value. **Concept-level wrapper-consumer pass (#555 Phase 3):** for a wrapper OUTPUT-CONTRACT change, after the literal old-name grep run a CONCEPT-LEVEL pass — grep for the semantic role the changed field/output plays (not just its literal name) across `scripts/` and `scripts/` consumers — before declaring "no consumer affected". **API-model integration-test scope (AC 5.1):** when the Issue touches API request models, include `tests/integration/` in the scope of any new integration-test AC — a request-model change is a contract change a unit test alone does not exercise. **Wiring-contract Ralph timing (AC 5.4):** for Issues introducing wiring contracts (orchestrator + systemd, CLI + service), Ralph must run BEFORE Stage 5 — not deferred until context budget allows; a wiring contract unverified by Ralph is the class Stage 5 should not be the first to catch. **Adversarial corpus at first test pass (#555 Phase 3):** for allowlist/blocklist/fail-loud gates AND approval/consent/name-detection classifiers, the FIRST test pass already includes the adversarial corpus (multi-message threads, non-Latin scripts, negated phrasing, re-edit-after-approval) — authored at write time, never first discovered at Ralph Opus (that discovery costs a restart-from-Tier-1).
3.2. **Pre-Ralph self-check (AC 4.11):** run `grep -nE "except.*continue|except.*pass|except Exception:|\bfallback\b|>/dev/null" <new-files>` — each hit MUST have an associated log/metric emit or be explicitly documented as a design-approved silent path.
3.5. **Test-prod call coverage audit at each phase boundary (Theme 9, #477 AC 3.5)**: BEFORE closing phase N via the Layer 1 MCP close-out (step 3a), the implementer runs the call-seam grep for every new public callable / response-payload field / config key added in phase N. Commands: (a) `grep -rnE "from <new-module> import|<new-module>\.<new-callable>" tests/` for every new public function/method; (b) `grep -rnE "<new-field>" tests/` for every new response-payload field; (c) `grep -rnE "<new-config-key>" tests/` for every new config key. **Empty grep on a new public callable / field / config key = FAIL — test seam missing**. Either add the test seam in this phase's commits OR document the explicit no-test-needed rationale (rare; usually valid only for trivial dataclasses / pure-data exports). Skip-clean if phase work is doc-only or wiring-only with no new public surface. Cross-reference: STANDARDS.md "Test-Prod Call Coverage Discipline" + sonnet-review.md "Test-Prod Call Coverage (Theme 9)" section.
3a. **Layer 1 — Phase-boundary checkbox close-out (AP #20 Tier A, MANDATORY in execute mode only)**: BEFORE moving from phase N to phase N+1, run `mcp__github-checkbox__update_issue_checkbox(issue_number, checkbox_text, evidence)` for every completed Tier A Acceptance Criteria checkbox in phase N. (Plan mode = read-only per CLAUDE.md; MCP write tools only fire when the user has explicitly entered execute mode via `/Leader 4` or `/SST3-solo` invocation.) Interleaved with the phase commits, not batched. Evidence MUST use canonical patterns from `../reference/tool-selection-guide.md` Example 2: file:line / commit hash / command+output / subagent RESULT comment-id. If the tool is deferred: `ToolSearch(query="select:mcp__github-checkbox__update_issue_checkbox,mcp__github-checkbox__get_issue_checkboxes")` first, then invoke. Comment-only progress is a direct AP #20 violation. **No phase boundary may be crossed with a Tier A `[ ]` box behind it.** (Canonical rule: ANTI-PATTERNS.md AP #20; evidence patterns: `../reference/tool-selection-guide.md` Example 2.) Complemented by Gate 1 Layer 3 final-check below. **MCP Checkbox Close-Out Protocol (AC 4.8):** (1) copy checkbox text verbatim from a fresh `get_issue_checkboxes` call — never retype from memory (backtick-pairing failures are silent); (2) for multi-line AC text with nested sub-bullets, pass only the first line (everything before the first `- (` sub-bullet) to the MCP tool; (3) close each AC checkpoint at the moment the phase commit lands, NOT batched at Ralph PASS; (4) on a 401 MCP token error, post a one-line operator alert on the issue (`[MCP-TOKEN-REFRESH NEEDED]`) and proceed via `gh issue edit --body-file` for body updates — never silently skip. **(AC 5.5) WARNING:** `gh issue edit --body-file` OVERWRITES the entire issue body, clobbering MCP-set checkbox `[x]` state — before using it, re-fetch the CURRENT body (or the `get_issue_checkboxes` state) and preserve every `[x]` mark in the file you pass, or the MCP-ticked ACs silently revert to `[ ]`; (5) fast-track / mini-issues: the AP #20 evidence trail is mandatory regardless of issue size. **[SKIP]-branch contract (AC 4.9):** `[SKIP]` branches in sample-invocation scripts must emit the exact re-run command and current git SHA, so the operator need not cross-reference handoff comments. **New Public Surface Self-Check (AC 4.12):** for every new public callable introduced in this phase, (1) run `grep -rn <name> <source-dirs>` and record the callers count + one production file:line as AC evidence; (2) answer three questions — lifecycle cleanup needed? audit trail exists? HTTP/serialisation tested end-to-end? — and block phase close if any answer is No with no documented rationale. **CSS instance (#555 Phase 4):** a new shortcode/partial emitting a NEW CSS class greps the stylesheet for that class at author time and confirms it maps to a real rule — regardless of the page's draft-gated status (the un-draft trigger that makes it visible is unaudited).
4. Do NOT stop between phases to ask "shall I continue?" — the answer is yes. Proceed.
5. On long multi-phase work, auto-`/handover` + compact + CONTINUE when remaining nears ~50% (~500K of 1M) — compaction is a continuation, not an artificial break; never operate below 50% remaining.
6. Run the Verification Loop (Gate 1 below) after all phases are complete. **Audit-phase freshness (MANDATORY first step)**: before ANY audit / wiring / `callers_of` / `impact` query in the Verification Loop, run `bash $SST3/sst3-code-update.sh` unconditionally — your solo-branch edits rendered any session-start snapshot stale. Same rule applies at the Stage 5 Post-Implementation Review audit phase (see playbook "Audit-phase freshness" bullet under "Stage 4 — Implementation (Verification Loop)"). Skip only if doc-only diff (exempts all graph checks).
6.5. **PRE-RALPH READINESS GATE (AC 4.12 — before dispatching Haiku):** (0) **Mechanical vacuity sweep (#567 AC 4.3):** run `bash $SST3/sst3-test-vacuity.sh` over the diff's test files (plus `--count-in <doc>` for any allowlist-shaped dict the diff touches) and FIX every finding BEFORE any reviewer is dispatched — vacuous assertions and over-reaching allowlist reasons are script-detectable; spending a reviewer on them is the measured waste class. Exit 1 = findings block Tier-1 dispatch; (1) call `mcp__github-checkbox__get_issue_checkboxes` and batch-tick all ACs whose deliverables are committed and verified; (2) pass the resulting verified count ("N/M checkboxes ticked — verified via get_issue_checkboxes") in the frozen-scope snippet given to each Ralph tier; (3) if any AC has pending operational evidence (queue job, multi-hour backtest, operator-runtime gate), mark it `[deferred-N/A: <boundary-named>]` and defer Ralph dispatch until the evidence window closes. Dispatching Ralph with unchecked completable ACs guarantees a Haiku restart from an AP #20 miss. (4) **Verdict-bearing AC timing (#555 Phase 3):** when an AC's headline deliverable is itself a computed verdict/recommendation, defer ticking that checkbox until Ralph PASSES — a mid-Ralph fix can flip the verdict and strand already-appended PoW evidence. (5) **Ride-along precondition (#555 Phase 3):** run `git merge-base --is-ancestor origin/<default> HEAD` and `git diff origin/<default>..HEAD --stat`; confirm every file in the diff is attributable to this issue's phases; flag any unexplained file as a possible ride-along commit BEFORE the first Ralph tier runs — three tiers reviewing a foreign commit is the expensive way to find it. (6) **Operator-assist minimisation (#555 Phase 3):** for an interactive-login AC (OAuth, browser session), don't park the whole AC on the operator — drive the remote TTY via tmux and relay the auth URL, leaving the operator only the browser-authentication step. (7) **Pre-Ralph content self-check (#555 Phase 4):** for a deliverable that states a taxonomy/thesis or carries confidence/vote/certainty labels, source-cross-check EVERY labelled instance / named case against the Stage-1 source BEFORE dispatching Ralph — content self-consistency, not just AC structure.
7. Run Ralph Review (Haiku → Sonnet → Opus) — all 3 tiers mandatory. Ralph Review is a CODE DELIVERY VERIFICATION pass — it confirms the issue's Acceptance Criteria were delivered. It is DISTINCT from Stage 5 adversarial audit. Ralph passing does NOT substitute for Stage 5 subagent swarm (TB-3 N36). Model-selection rationale for the three Ralph tiers (Haiku surface / Sonnet logic / Opus deep): see `docs/research/model-selection-haiku-4-5.md`. **Terminology note**: Ralph "tiers" (sequential Haiku → Sonnet → Opus, restart on fail) are distinct from Stage 5 subagent "layers" (parallel Layer 1 + Layer 2, different angles per AP #14). Ralph is code-delivery verification; Stage 5 layers are post-implementation adversarial audit. AP #20 "Layer 1/2/3" enforcement in this skill body (step 3a + pre-Gate-1 paragraph + Gate 1 bullet) is a THIRD separate use of "Layer" terminology — execution-time enforcement gates, not review-time subagent dispatches. **Tier 3 Opus raw-tool counter-query rule (#447 Phase 5)**: when ANY Tier 3 finding depends on `../scripts/sst3-code-*.sh` output (recall counts, large-fn lists, callers, blast-radius, untested-py results), Opus MUST dispatch ≥1 raw-tool counter-query subagent on the same target before signing PASS. The counter-query result + delta vs wrapper-lane output is recorded in the Tier 3 review comment. Skipping this when wrapper-lane output is load-bearing = Tier 3 FAIL. **Reviewer Isolation (#555 Phase 3):** every Ralph tier prompt pins formatter/linter invocations to their read-only check form and bans mutating git ops (merge/checkout/stash/rebase/reset) — reviewers inspect, never mutate; a review pass must not pollute the Gate-2 index (full precondition text in each `ralph/*.md` header).
8. On Ralph FAIL: fix → restart from Tier 1, up to 3 restarts. Restart 4: NOT taken — escalate to a class-sweep (deterministic enumeration preferred where the class is mechanically enumerable; see `standards/stage-4/mutation-verification.md`), then resume Ralph with the count reset to zero for exactly ONE further loop; if that loop does not PASS, STOP and report every outstanding finding with its class to the operator — terminal state, never a silent abandon (#567, canonical: `standards/stage-4/ralph-review.md`). On Ralph PASS: proceed to Gate 1. **Post-pass rework re-validation (#555 Phase 3):** when the operator requests a material change AFTER Ralph passed, re-run Ralph Tier 2/3 scoped to the rework diff before relying on Stage 5 alone — Stage 5 is adversarial audit, not code-delivery verification of net-new post-pass changes. **Signal the counter at both boundaries — it cannot observe either for itself**: `bash ~/.claude/hooks/sst3-ralph-restart-counter.sh --restart` when you restart, and `bash ~/.claude/hooks/sst3-ralph-restart-counter.sh --escalate` when you escalate (the only thing that resets the count). Left unsignalled the count stays **0** — not merely low. Every tier dispatches under the same agent type, so the event stream cannot tell a restart from an ordinary tier event and the counter derives nothing from it; step 9's mandatory `ralph_restarts: N` then reports 0 forever and the bound is never observed to be reached.
9. Post checkpoint to issue comment when implementation is complete. **Every phase checkpoint MUST carry a `ralph_restarts: N` line**, read from the counter (`bash ~/.claude/hooks/sst3-ralph-restart-counter.sh --get`) — never estimated, and never written as `0` when the counter reports `RALPH-COUNTER INERT`; report the inert state instead, because a silent zero reads as "the bound has not been approached". This is the surface that actually fires per phase, which is why the count is surfaced here rather than only in the session-handover template (whose triggers are ~50%-context / token-limit / manual, never per-phase). **PoW precision (#555 Phase 4):** any 'N tests' PoW claim pastes the `pytest --collect-only -q` (or language-equivalent) count — never a hand-sum.

**Layer 2 — Pre-Gate-1 double-check (MANDATORY before entering the Verification Loop)**: if the tool is deferred, `ToolSearch(query="select:mcp__github-checkbox__get_issue_checkboxes,mcp__github-checkbox__update_issue_checkbox")` first. Then run `mcp__github-checkbox__get_issue_checkboxes` and confirm every phase-completed Tier A checkbox is already `[x]` with canonical evidence (file:line / commit hash / command+output / subagent RESULT comment-id per `../reference/tool-selection-guide.md` Example 2). If any Tier A box corresponds to completed work but is still `[ ]`, close it NOW via `update_issue_checkbox` with canonical evidence — do NOT defer to Gate 1. Gate 1 is the FINAL check, not the first; it must enter from a clean baseline. (Complements Layer 1 at phase-boundary above; Gate 1 below is Layer 3 final-check.)

**Gate-1/Gate-2 Mechanical Pre-flight (AC 4.7).** Gate-1 entry pre-flight (run before ANY Verification Loop check): (a) for every AC naming a file artifact, run `test -f <path>` — any missing path = FAIL before continuing; (b) run `wc -c <issue-body-file>` — if the body exceeds 120000 bytes, use `gh issue edit --body-file` for all body updates (the MCP github-checkbox tool hits the kernel single-arg ceiling above this threshold); (c) if any wrapper-lane script exits engine-missing, log `[WRAPPER-LANE unavailable: <reason>]` and cross-reference a tracking issue before continuing.

### Gate 1: Verification Loop

> Canonical: ../workflow/WORKFLOW.md "## Verification Loop" — run that loop here. The Leader-specific gates below extend it; the generic checks (all-checkboxes-evidence / Overengineering / Architecture reuse / Code duplication / Fallback policy / Wiring / Quality scan / Ralph 3-tier confirmation) live in the canonical and MUST NOT be restated here (Cut #3, AC 1.6).

- [ ] **Graph-backed diff audit (MANDATORY when graph available per STANDARDS.md "Structural Code Queries")**: read `graph_applicable` from Stage 1 research file — do NOT re-classify (TB-2 carry-forward). If `graph_applicable=false`, skip this checkbox and proceed; if `true`, run `bash $SST3/sst3-code-review.sh <default-branch>` on the diff — always wrapped in a subagent with an UPFRONT warning in the prompt: "Expect 4MB+ JSON output — pipe to file + use `jq`/`python` to slice. Do NOT try to read the full payload." (Round-5 overflow data: 7× across 3 rounds / 144K-11.7M chars.) Feed the returned per-file `impacted_callers` counts + untested-function list (+ any `review-error` records — engine failures are findings, not noise; #544) into the swarm scope. This determines swarm size + angles, NOT replaces subagents. Subagents still handle semantic / intent / cross-document angles per AP #19 12-moments carve-out. If `review` output surfaces high-`impacted_callers` (wide-blast-radius) files, PRE-ASSIGN Layer-1 subagent angles by impacted hub (one subagent per hub) rather than generic-angle coverage. For hybrid diffs (code + docs): run `review` on the full diff — markdown/YAML/JSON files yield no impact edges, and `review` output tolerates that. **Internal-consistency check (#447 Phase 5)**: if the `review` JSON's `impact` array is EMPTY but the diff is non-trivial (>5 functions touched per `sst3-code-impact.sh`), treat the wrapper output as SUSPECT — dispatch ONE raw-tool subagent (grep / ast-grep direct) to compute the impact independently, compare, and gate on the delta. An empty review JSON paired with a real diff is a known wrapper failure mode that the self-test gate cannot fully cover (engine-broken vs engine-missing on the orchestrator path).
- [ ] **Layer 3 — Checkbox-MCP coverage gate (AP #20 FINAL CHECK)**: per canonical definition in `../workflow/WORKFLOW.md` "Verification Loop" — **(0) Check if post-commit hook + GHA already auto-ticked Phase M Tier-A boxes** (#477 Phase 6 AC 6.6 — Theme 6): inspect `SST3-metrics/.tier-a-auto-tick/<issue#>-<phase>.json` and the Issue body's most-recent edits — if every Tier-A box for the just-completed phase is already `[x]` with a `PoW [<ac_id>]: ... (auto-ticked via tier-a-auto-tick.yml)` line, document in checkpoint comment ("Tier-A auto-tick processor closed N/N boxes for Phase M") and skip directly to step (4) re-verification. If no (hook unavailable / GHA disabled / sentinel never written / box-text drift caused matcher miss / network failure), proceed with manual MCP invocation in steps (1)-(3) as before. Manual MCP override remains the AP #20 fallback. Then: (1) `ToolSearch(query="select:mcp__github-checkbox__get_issue_checkboxes,mcp__github-checkbox__update_issue_checkbox")` if deferred; (2) run `get_issue_checkboxes` and list every Tier A box still `[ ]` that corresponds to completed work; (3) for each such box, invoke `update_issue_checkbox(issue_number, exact_checkbox_text, evidence)` with canonical evidence (file:line / commit hash / command+output / subagent RESULT comment-id per `../reference/tool-selection-guide.md` Example 2); (4) re-run `get_issue_checkboxes` and confirm every Tier A box is now `[x]`. Comment-only progress = FAIL. Tier B batched-closures applied here are acceptable per Phase 9 cadence (ANTI-PATTERNS.md AP #20). Do NOT duplicate the rule here — WORKFLOW.md is canonical; only the procedure is expanded.
- [ ] **Three-Tier test gate (Unit / Workflow / E2E)** — per WORKFLOW.md "Verification Loop" canonical tier checkboxes (#484 T4.3): BUILD = all 3 tiers' tests exist for the change; USE = all 3 RUN (fire & pass) every change (operator directive dotfiles#528 — supersedes scope-matched fire; E2E may be sized to a small backtest / execution+cleanup but still RUNS; the only non-run is a documented `structural-inapplicable: <reason>`). Record the tier-evidence line `tiers: U=.. W=.. E2E=.. M=.. | BUILD-evidence:<file:line per tier>` (defined once in STANDARDS.md; `M=` is the #567 mutation-verification result — `M=unproven` blocks like a fail). The project test suite ("no regressions") = the union of the checked-in Unit + Workflow + E2E tests, not any single tier. WORKFLOW.md is canonical — do NOT re-define the spec here. **Live-mutation safety + gated-collection proof (#555 Phase 4):** before running ANY test file in this gate, grep it for live-service-mutation markers (`skipif.*is-active`, `systemctl restart`) — if present, run it ISOLATED and restore+verify service state after; when a test file lives under a conftest-gated dir, grep the conftest for collection-time markers/skips FIRST and prove the new file runs under the gated-off condition (a dry-run collection count is part of the deliverable).
- [ ] **AP #18 sample-invocation = the Workflow Tier of the Three-Tier test gate above** (#484 T4.3): the real-CLI sample invocation, ≥3-repo-shape + raw-tool counter-query, row-count + downstream-consumer + wrapper-vs-raw-delta verification, exit-0-insufficient, and explicit `call_args.kwargs[...]` mock-assertion rules are the Workflow-Tier USE clause. Canonical spec: WORKFLOW.md "Verification Loop" Workflow Tier checkbox + ANTI-PATTERNS.md AP #18 + STANDARDS.md "Three-Tier Testing Framework". Leader defers — do NOT duplicate the spec here.
- [ ] **VL-1/VL-2/VL-3/VL-4 sub-checks (AC 4.6)** — per WORKFLOW.md Verification Loop extended sub-checks. Config/infra shapes additionally require an actual `workflow_dispatch` or PR-trigger CI run (not just a construction proof).
<!-- Gate, not audit — comprehensive-walk template per AP #23 does not apply (verifies named conditions, not multi-section canonical). -->
- [ ] **Skill-canonical verification gate** (N32 Double-Guardrail — per Guardrails principle): run the invoked skill's own verification checks. Examples: `check-ai-writing-tells.py` exit 0 (voice / blog / CV / voice-doc-repo); eBay 21-field count grep + SMART test evidence; claude-api prompt-caching verify; project-specific pre-commit hooks green. Evidence: pass/fail + output in issue comment.
- [ ] **Mirror-lane verification (Lane A + Lane B)** — per WORKFLOW.md "Verification Loop" Mirror-lane verification rule (#460 Phase 8 W5): canonical authoritative; do NOT duplicate the spec here.
- [ ] **Mirror-lane Lane A + Lane B** — `--dry-run` and `--validate` are mutually exclusive in propagate-mirrors.py argparse, so they run as separate commands. **Gating invocations** (exit code is load-bearing): (1) `python3 $SST3/propagate-template.py --all --dry-run` (Lane A), (2) `python3 $SST3/propagate-mirrors.py --validate` (Lane B). **Report-only** (run it, but do NOT cite its exit code as evidence): `python3 $SST3/propagate-mirrors.py --dry-run` — `do_dry_run` returns `EXIT_OK` unconditionally after printing "N would change", so it exits 0 with drift present; an earlier version of this checkbox demanded "three separate invocations all exit 0", which made one third of the gate vacuous (#552 Ralph round 9, verified by executing it against a drifted tree). Skip-clean if no canonical-mirrored surface touched.
- [ ] **Doc-lane diff-trigger (#484 W6.1 — NOT graph-gated)**: if this Issue's diff touches `*.md` / frontmatter / docs, the doc-lane (`sst3-doc-lint` / `sst3-doc-links` / `sst3-doc-frontmatter`) MUST have run regardless of `graph_applicable` (a `graph_applicable=false` semantic/governance Issue is NOT exempt — its `*.md` diff still gets linted). Canonical rule + rationale: WORKFLOW.md Stage 1 "Doc-lane is diff-triggered, NOT graph-gated"; Leader defers — do NOT re-define here. Skip-clean only if the diff touches no doc file.

If ANY check fails: fix, re-run ALL checks. The loop exits only when every check passes.

### Gate 2: Merge (recursion-safe remote fast-forward — dotfiles#488 AC 1.3)

The pre-#488 block performed a shared-tree branch-switch + pull + local-merge + push to main — the exact shared-HEAD mutation this Issue fixes (a shared-clone branch-switch moves every concurrent agent's HEAD). It is replaced with a pinned remote merge that touches **no shared working tree**:

1. Ensure all work is committed (per-file commits) by exact pathspec. **(AC 4.7) (d)** Detect the default branch dynamically: `git remote show origin | sed -n 's/.*HEAD branch: //p'` — never hardcode `master`/`main`. **(e)** Before any rebase, predict conflicts: `comm -12 <(git diff --name-only HEAD | sort) <(git diff --name-only origin/$(git remote show origin | sed -n 's/.*HEAD branch: //p') | sort)` must return zero lines.
2. From the worktree: `git push origin <solo-branch>` (publishes the branch).
3. Server-side fast-forward of `origin/master`: `git push origin <solo-branch>:master`.
   - On non-fast-forward rejection (the documented transient pre-push race — `origin/master` advanced; post-fix-B there is no 289-row index to corrupt so this is an ordinary push race that self-heals): `git fetch origin master`, then **inside the worktree** `git rebase origin/master` (preserving BOTH sets of changes — Solo Branch Merge Safety), then retry step 3. Bounded ≤3 attempts. **NEVER** `--force` / `--force-with-lease`.
   - This gate runs ONLY `git push` / `git fetch` / `git rebase` inside the isolated worktree. It NEVER branch-switches, local-merges, or resets the shared main working tree (the chokepoint #488 removes).
4. `ExitWorktree action:keep` until the push is confirmed landed (`git ls-remote origin master` == solo tip), then `ExitWorktree action:remove`; `git push origin --delete <solo-branch>`; `git fetch --prune`. **Pre-remove artefact copy (#555 Phase 3):** before `action:remove`, copy any issue-referenced gitignored artefacts (logs/, PoW screenshots) from the worktree into the canonical clone's equivalent path — removal deletes them with the worktree. (.env provisioning is already handled by `scripts/setup-worktree-deps.sh` — do not re-add it.)

### Gate 3: User Review Checklist

1. Read `../templates/user-review-checklist.md`. Use the TEMPLATE. Do NOT invent your own.
2. Fill in ALL 10 sections. None are optional:
   - Scope Verification
   - Context Checkpoint
   - Gap Analysis — No Excuses Gate
   - How It Works
   - Cross-Issue Impact
   - Discoverability Check
   - Fail Fast Audit
   - Modular Architecture Review
   - Post-Implementation Review Gate
   - Closure Gate
3. Post the completed checklist as a comment on the GitHub issue.
4. Report to user: what was merged, issue URL, checklist posted.

**DO**: Read the issue thoroughly. Implement all phases. Commit per file. Push frequently. Run Ralph Review. Run all 3 gates sequentially. Use the template files. Verify every step end-to-end. Keep going until done.
**DON'T**: Stop to ask permission between phases. Run below 50% remaining without auto-`/handover` + compact on long work. Skim the issue. Leave phases incomplete. Fire-and-forget commits without verifying push succeeded. Skip the Verification Loop. Make up your own checklist. Defer the merge. Merge without Ralph evidence.

**Per-stage feedback (Layer C; per STANDARDS.md §Per-Stage Feedback Capture)**: write the Stage 4 block to the per-issue feedback file before declaring complete. Stage 4 sub-structure: `worked` / `didnt` carry per-Ralph-tier sub-bullets; `friction` records `ralph_restarts: <N>` + per-tier outcomes.

**SIGN-OFF**: When done, tell the user: "Stage 4 (Implement) complete. All phases implemented, Verification Loop passed, Ralph Review passed, merged to main, user-review-checklist posted. Next: `/Leader 5` (Post-Implementation Review — 100% mandatory subagent swarm)."

---

## Stage 5 — Post-Implementation Review (Subagent Swarm)

**Stage 5 mandatory-reading directive (#498 Phase 4 AC 4.8)**: run `bash $SST3/load-stage-rules.sh 5` to load the Stage-5-tagged subset (adversarial-audit / completeness-gate / regression / wrapper-vs-raw delta / §3-deferral re-litigation rules). Replaces full-canon read at Stage 5 entry. Non-zero exit = STOP; do not proceed on partial canon.


**Procedure (relocated — #555 AC 2.6)**: the full Stage-5 process (audit doctrine + steps 0–8b + DO/DON'T) lives in `standards/stage-5/post-implementation-review-procedure.md` and is EMITTED by the Stage-5 canon load above — follow it step-by-step from that emit; do NOT reconstruct it from memory. A non-zero loader exit means you have NO procedure: STOP.


**Per-stage feedback (Layer C; per STANDARDS.md §Per-Stage Feedback Capture)**: write the Stage 5 block to the per-issue feedback file before declaring complete (10 fields per STANDARDS.md). After writing, update frontmatter `stages_logged` to include 5 + bump `last_updated`; aggregator regenerates index automatically on next commit.

**SIGN-OFF**: When done, tell the user: "Stage 5 (Post-Implementation Review) complete. All work audited, fixes applied, regression tests passed. Issue [URL] is done. Close the issue when satisfied."
